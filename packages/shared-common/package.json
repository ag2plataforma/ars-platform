{
  "name": "@ars-platform/shared-common",
  "version": "0.1.0",
  "description": "Libreria compartida real para todos los servicios: maquina de estados, motor de reglas, auditoria, autenticacion JWT, envio de correo.",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"(pendiente configurar eslint)\"",
    "verify:rules-engine": "node scripts/verify-rules-engine.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.0",
    "@nestjs/config": "^3.3.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/passport": "^10.0.3",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/passport-jwt": "^4.0.1",
    "typescript": "^5.5.0"
  }
}
