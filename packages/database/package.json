{
  "name": "@ars-platform/database",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente Prisma compartido, generado por introspecci\u00f3n del esquema ars_platform. Todos los servicios importan el cliente desde aqui en vez de tener su propia copia.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:pull": "prisma db pull",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:seed-admin": "node scripts/seed-admin-user.js",
    "build": "prisma generate && tsc -p tsconfig.json",
    "postinstall": "prisma generate",
    "db:seed-example-rules": "node scripts/seed-example-rules.js"
  },
  "dependencies": {
    "@ars-platform/shared-common": "*",
    "@nestjs/common": "^10.4.0",
    "@prisma/client": "^5.20.0",
    "bcryptjs": "^2.4.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "typescript": "^5.5.0"
  }
}
