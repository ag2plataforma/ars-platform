# Roadmap por fases

Estado a 2026-09-18.

## Fase 0 — Descubrimiento ✅ (completada)

- Auditoría del backend actual (20 repos, versiones, patrones, duplicación).
- Auditoría del frontend actual (2 apps Angular, madurez, gaps).
- Extracción y análisis de las 44 funciones PL/pgSQL (motor de negocio real).
- Decisiones de arquitectura tomadas (ver `00-arquitectura.md`).

## Fase 1 — Fundación técnica (en curso)

- [x] Estructura del monorepo (`package.json`, workspaces, `tsconfig.base.json`).
- [x] `packages/shared-common` scaffoldeado (máquina de estados y motor de reglas como esqueleto, interceptor de auditoría funcional).
- [x] `services/iam-service` scaffoldeado como plantilla de referencia.
- [x] Esquema `ars_platform` creado en Postgres (aislado de `entity`/v1) a partir del DDL de las 130 tablas.
- [x] `packages/database`: Prisma conectado por introspección (130 modelos generados), `PrismaService`/`PrismaModule` y `PrismaStateRuleRepository` implementados.
- [x] Máquina de estados validada end-to-end contra datos reales (`iam-service`, endpoints de prueba en `/state-machine/*`) — adelantado desde Fase 2 porque era el mayor riesgo técnico.
- [x] Ciclo de desarrollo (`npm run start:iam`: build de libs + build del servicio + `node dist/main.js`) validado y estable. Dos causas raíz encontradas y corregidas en el camino: (1) faltaba `tsconfig.build.json` en el servicio — `nest build` lo busca por convención y sin él compila "0 archivos" en silencio (exit 0, sin `dist`); (2) `StateMachineModule` de `shared-common` necesitaba ser un módulo dinámico (`forRoot(repositoryProvider)`) para que el binding de `STATE_RULE_REPOSITORY`, hecho en el servicio consumidor, quedara en el mismo módulo que `StateMachineService` — Nest no resuelve providers "hacia arriba" en módulos que lo importan. Detalle completo en el `README.md` raíz.
- [x] Autenticación JWT real (`iam-service`): `POST /auth/login` contra `TUser`/`TUserCredential` (bcrypt), guard global en `shared-common` (`AuthModule`/`JwtAuthGuard`) que protege toda ruta salvo `@Public()` — reutilizable por cualquier servicio nuevo con solo importar el módulo. Script `db:seed-admin` para crear el primer usuario de prueba. Validado end-to-end (sin token → 401, con token válido → 200 con datos reales). Tercera causa raíz encontrada en el camino: `ConfigModule.forRoot({ envFilePath: '.env' })` con ruta relativa se resuelve contra el `cwd` del proceso, no la carpeta del servicio — y `start:iam` corre `node dist/main.js` desde la raíz del repo, así que nunca encontraba el `.env` real; se resolvió usando `join(__dirname, '..', '.env')`.
- [x] Gestión de usuarios completa (`iam-service`): CRUD de `TUser`/`TRol` (`/users`, rol `ADMIN`), cambio de contraseña, y recuperación de contraseña por correo (Brevo) con token de un solo uso auto-invalidante (sin tabla nueva en la BD). Guard de roles genérico (`@Roles`/`RolesGuard` en `shared-common`) y puerto de envío de correo (`EMAIL_SENDER`/`EmailSender`, implementación Brevo) reutilizables por cualquier servicio futuro. Doble factor de autenticación diferido deliberadamente a una fase posterior (decisión explícita — v1 tampoco lo tenía).
- [x] Motor de reglas de cálculo real (`shared-common`), equivalente a `FQuoteCoverageConcept`/`FMovementConcept` + `FGetValueAttribute`/`FGetValueRule` (ver `docs/01-especificacion-motor-negocio-actual.md`, §3) — adelantado desde Fase 2 por ser, junto con la máquina de estados, la otra pieza central del sistema y el bloqueante real para `product-rating-service`/`underwriting-service`. El `EXECUTE` de SQL dinámico del original se reemplazó por un evaluador de expresiones propio (tokenizer + parser recursivo-descendente + evaluación de AST, sin `eval`/`new Function`/SQL dinámico — `formula-expression.ts`), sin agregar ninguna dependencia nueva. Sustitución de custom fields por límite de palabra (mejora de seguridad sobre el `strpos`/substring original, documentada en §6 de la especificación) y de referencias `rule('COD')` primero contra lo ya calculado en la misma cadena (en memoria) y solo si no está ahí contra la BD — replica el efecto de que el original persiste cada regla dentro del mismo loop antes de evaluar la siguiente, sin que este motor tenga que escribir en la BD (queda puro y testeable; la aplicación del resultado — actualizar columna o insertar concepto — la hace el servicio consumidor). Implementación real de los tres puertos (`CalculationRuleRepository`/`AttributeValueResolver`/`RuleValueResolver`) contra Postgres en `@ars-platform/database`, mismo patrón `forRoot()` que `StateMachineModule`. Verificado con `npm run verify:rules-engine` (evaluador puro + cadena `PrimaNeta -> Impuesto -> PrimaTotal` con dependencias en memoria, sin BD — no hay todavía `SCalculationRule` configuradas en `ars_platform` para una validación end-to-end contra datos reales; eso llega en Fase 2 con `product-rating-service`).
- [x] `product-rating-service` scaffoldeado siguiendo exactamente la plantilla de `iam-service` (mismo `tsconfig.build.json`/`nest-cli.json`/patrón de `ConfigModule`), con el motor de reglas de cálculo ya conectado a Postgres real (`ProductRatingRulesEngineModule` usando `RulesEngineModule.forRoot(...)` con las implementaciones de `@ars-platform/database`) y expuesto en `GET /rules-engine/applicable-rules` y `POST /rules-engine/evaluate` para probarlo en cuanto existan `SCalculationRule` reales configuradas (hoy `ars_platform` no tiene datos de configuración de negocio, solo estructura). Ver `services/product-rating-service/README.md`.
- [x] Script `db:seed-example-rules` (`packages/database/scripts/`): crea la cadena mínima de configuración que `SCalculationRule` exige (17 tablas de catálogo/producto, todo con códigos `SEED_...`) y tres reglas encadenadas (`PrimaNeta -> Impuesto -> PrimaTotal`), para validar `/rules-engine/*` de `product-rating-service` contra Postgres real sin depender de ningún otro servicio. Idempotente. Sirve también como referencia concreta de qué hace falta para dar de alta una regla, hasta que exista el CRUD real (Fase 2). **Validado end-to-end por el usuario**: `GET /rules-engine/applicable-rules` devolvió las 3 reglas ordenadas por `Order` con su `FormulaJSON` real, y `POST /rules-engine/evaluate` devolvió exactamente PrimaNeta=100, Impuesto=16, PrimaTotal=116 con `columnName: "Prime"` — primera prueba real (no en memoria) de todo el motor de reglas contra Postgres.
- [x] CRUD real de los 8 catálogos "simples" en `product-rating-service` (`SRiskLevel`, `SRisk`, `SRiskType`, `SCurrency`, `SInsuranceArea`, `SInsuranceLine`, `SDeductibleType`, `SLimitType`). Lógica Create/List/Get/Update/SetState concentrada en una clase genérica (`CatalogCrudService<T>`, `src/catalogs/catalog-crud.service.ts`) para no repetirla 8 veces; cada catálogo aporta un servicio delgado que resuelve sus FKs propias por código (`SRisk.codRiskLevel`, `SInsuranceLine.codInsuranceArea`) y, en `SRiskLevel`/`SInsuranceArea`, su jerarquía opcional (`codXParent`). Lectura abierta a cualquier usuario autenticado; creación/edición/cambio de estado restringidos a `@Roles('ADMIN')`. Ver `services/product-rating-service/README.md` para el detalle de rutas.
- [x] CRUD real de las 7 entidades "de dominio" en `product-rating-service` (`SProduct`, `SRiskProduct`, `SPlanProduct`, `SPlanProductRisk`, `SCoverage`, `SCoveragePlan`, `SCalculationRule`) — reemplaza el atajo de `db:seed-example-rules` (que queda como ejemplo/semilla rápida) y es la respuesta definitiva a "cómo se registra una fórmula nueva". Las 4 entidades con `Cod`/`Des` único (`SProduct`, `SPlanProduct`, `SCoverage`, `SCalculationRule`) reutilizan `CatalogCrudService<T>` (extendida con un `include` opcional para traer relaciones resueltas); las 3 que son tablas de unión/configuración sin código propio (`SRiskProduct`, `SPlanProductRisk`, `SCoveragePlan`) se escriben explícitas, mismo estilo que `UsersService` en `iam-service`. `SCalculationRule` valida la forma de `FormulaJSON` (`{ if, then, else }`) al guardar, antes de que el motor de reglas la encuentre mal formada al evaluar. Mismo criterio de permisos que los catálogos. Ver `services/product-rating-service/README.md` para el orden de creación (Producto → RiskProduct → PlanProduct → PlanProductRisk → CoveragePlan → CalculationRule; Coverage es independiente) y el detalle de rutas.
- [x] CRUD real de tablas de tarifa multidimensional en `product-rating-service` (`SRateTable`, `SRateFactor`, `SRateValue`, hasta 5 factores por tabla). `SRateTable` reutiliza `CatalogCrudService<T>` (con `include` anidado para traer sus `SRateFactor` ya ordenados por `numOrder`); `SRateFactor`/`SRateValue` se escriben explícitos (sin `Cod`/`Des` propio). `SRateFactor` valida `numOrder` entre 1 y 5 sin repetir posición dentro de la misma tabla (regla de negocio, no constraint de BD). Mismo criterio de permisos que los catálogos. Ver `services/product-rating-service/README.md`.
- [x] Equivalente exacto a `FGetRateValue`, confirmado contra el código fuente real de la función legacy en Postgres (script nuevo `packages/database/scripts/find-legacy-function.js`, reusable para cualquier función legacy futura). Resultado clave: NO hay criterio de desempate por especificidad — el original exige un calce exacto en `Factor1`, comodín solo del lado de quien llama (no de la fila) para `Factor2..5`, y falla si encuentra 0 o más de 1 fila, en vez de elegir una "más específica". Tampoco filtra por vigencia (`TstInit`/`TstEnd`) pese a que la tabla las tiene — se replicó tal cual. Una sola implementación (`PrismaRateValueResolver` en `@ars-platform/database`) sirve dos usos: el endpoint de prueba `GET /rate-values/lookup` en `product-rating-service` y el puerto `RATE_VALUE_RESOLVER` del motor de reglas. Detalle completo en el README de `product-rating-service`.
- [x] `FGetRateValue` cableado dentro del evaluador de fórmulas (`packages/shared-common`) — una fórmula de `SCalculationRule` ya puede invocarlo con `FGetRateValue('CODTABLE','F1','F2',NULL,NULL,NULL)` (misma firma posicional que el original). Verificado sin BD en `npm run verify:rules-engine`. Con esto, `product-rating-service` cubre el 100% de lo documentado en `docs/01-especificacion-motor-negocio-actual.md` §3.
- [x] Scaffolding de los servicios restantes (`party-service`, `reference-data-service`, `underwriting-service`, `claims-service`, `billing-service`, `gateway`), misma plantilla que `iam-service`/`product-rating-service` (`ConfigModule` con ruta absoluta al `.env`, `AuthModule` global, `GET /health`). Todos compilan limpio con `nest build` (verificado con el toolchain real, no solo tipos) y ya están enlazados en los npm workspaces (`npm run start:<nombre>`/`:watch`, ver root `package.json`). Única diferencia entre ellos: `gateway` NO importa `PrismaModule` — es un BFF sin modelo de datos propio, enruta y agrega respuestas de los otros servicios en vez de consultar Postgres directamente. Sin lógica de negocio todavía en ninguno (eso es Fase 2, ver abajo); cada uno conserva su `README.md` con el alcance funcional ya documentado en Fase 0.
- [x] Pipeline de CI básico en GitHub Actions (`.github/workflows/ci.yml`): en cada push/PR a `main` corre `npm ci`, build de `packages/shared-common`/`packages/database`, build de los 8 servicios y lint/test en todos los workspaces (hoy placeholders donde aún no hay reglas de lint ni tests reales configurados). **Validado end-to-end**: primer run real en GitHub Actions exitoso (https://github.com/ag2plataforma/ars-platform/actions/runs/35069730999), confirmando que `prisma generate` funciona sin problema en el runner (a diferencia del sandbox de desarrollo, que no tiene salida de red hacia `binaries.prisma.sh`). De paso se detectó y corrigió que `package-lock.json` había quedado desincronizado con los 6 servicios scaffoldeados en el commit anterior (nunca se completó un `npm install` con red completa desde que se crearon), lo que habría hecho fallar `npm ci` en CI.
- [x] Entorno de despliegue funcionando end-to-end con `iam-service` (hello world real desplegado). Camino preparado para Oracle + Coolify (ver `docs/03-despliegue-oracle-coolify.md`), pero bloqueado en la práctica por falta de capacidad de Oracle para el shape `VM.Standard.A1.Flex` (`Out of capacity...`, un problema de disponibilidad de Oracle, no de configuración). Decisión (2026-09-18): perseguir en paralelo (a) Render como camino de despliegue real con el mismo Dockerfile (ver `docs/04-despliegue-render.md`) y (b) un reintento automático de la capacidad Oracle vía GitHub Actions, para migrar a Oracle+Coolify si la instancia se llega a conseguir. En el camino se encontró y corrigió un segundo problema real, esta vez de Prisma: los 8 `Dockerfile` (con `node:20-alpine`) chocaban con un bug confirmado de Prisma en Alpine 3.21+ que rompe la detección de OpenSSL (sin fix hasta Prisma 6.1.0) -- se migraron a `node:20-bookworm-slim`. **Desplegado y verificado (2026-09-18)**: `iam-service` en Render (`https://ars-iam-service.onrender.com`), `GET /health` responde 200 y `POST /auth/login` con el usuario admin real devuelve un token válido -- confirma que el contenedor desplegado pega de verdad contra la misma base Postgres real. Reintento de Oracle A1.Flex sigue pendiente de configurar, para migrar más adelante si consigue capacidad.
- [x] Decisión sobre partición del esquema de base de datos por servicio: se mantiene un único esquema compartido (`ars_platform`) para todos los servicios durante toda la transición -- ya es lo que ocurre en la práctica (`iam-service`, `product-rating-service` y `reference-data-service` comparten el mismo cliente Prisma de `@ars-platform/database`) y separarlo no aporta nada hoy (un solo desarrollador, sin necesidad real de aislamiento entre servicios) a cambio de complejidad real (migraciones coordinadas, límites de transacciones cross-schema). Se revisita solo si aparece una razón concreta (no por "prolijidad" arquitectónica) más adelante.

## Fase 2 — Núcleo asegurador (paridad funcional)

- [x] CRUD real de `SFieldDictionary`/`SFieldValue` en `reference-data-service` (su primer módulo de negocio real, `FieldCatalogModule`) -- mismo patrón `CatalogCrudService` que product-rating-service, movido a `@ars-platform/shared-common` en el camino (ya lo consumen dos servicios). **Corrección de alcance sobre lo que decía este ítem antes**: al investigar el esquema real, `SAttribute` NO es un simple catálogo de valores -- es la primera pieza de un motor de atributos personalizables/flujos configurables bastante más grande (`SAttribute` -> `SAttributeProperty` -> `SModelAttribute`, este último enlazado con `SEntity`/`SFlowStep`, y consumido en tiempo de ejecución vía el JSON `RiskAttributeValue` de `TQuoteRisk`/`TFileRisk` -- ver `PrismaAttributeValueResolver` en `@ars-platform/database`, que ya implementa la lectura de ese lado). Implementarlo bien exige su propia investigación dedicada primero (mismo criterio que se usó con `FGetRateValue`: no adivinar la forma), así que queda como un ítem separado más abajo en vez de mezclarlo con el diccionario de campos, que sí estaba autocontenido y ya quedó resuelto.
- [x] Investigación del motor de atributos personalizables (`packages/database/scripts/investigate-attribute-engine.js`, corrido contra la BD real). Hallazgos clave: (1) `FGetValueAttribute` confirma exactamente lo que ya implementa `PrismaAttributeValueResolver` en `@ars-platform/database` -- sin cambios de código ahí, el `attribute('COD')` del motor de reglas ya es correcto contra el original. (2) Cadena real de configuración, con datos de ejemplo: `SModelAttribute` (qué set de atributos aplica a qué entidad/producto, ej. "Perro"/"BikeMountain") -> `SAttributeProperty` (el campo de formulario real por producto, con JSON `{name,label,type,validators,...}`) -> `SAttribute` (concepto reutilizable, ej. "Raza del Perro", ligado a un `SFieldDictionary`) -> `SFieldValue` (resuelve el uuid guardado en `RiskAttributeValue` a un código legible). (3) El hack `replace('#','Bicicleta')` de `FFlowStep` resultó ser código de debug abandonado, no una regla de negocio -- ver `docs/01-especificacion-motor-negocio-actual.md` §5/§6, ya actualizado con el detalle. (4) Confirmado un typo real (`FGeState`) en `FGetProcessFlowByUIDSession`, código roto/no usado. **Decisión de alcance**: separar la parte de *configuración* (`SAttribute`/`SAttributeProperty`/`SModelAttribute`, autocontenida y ya bien entendida) de la parte de *orquestación del wizard* (`SEntity`/`SFlowStep`/`FPInstanceFlow`, que depende de las pantallas reales de cotización que todavía no existen en `underwriting-service`) -- ver los dos ítems siguientes.
- [x] CRUD de configuración del motor de atributos (`SAttribute`, `SAttributeProperty`, `SModelAttribute`, `SEntity` de solo lectura) en `reference-data-service`, módulo `AttributeEngineModule`. Mismo patrón `CatalogCrudService` (las tres tienen forma `Cod`/`Des`/`IdeState`+auditoría) con una capa de resolución de códigos a ids igual que `field-values.service.ts`. `IdeReference` en `SModelAttribute` es polimórfico -- se acepta como uuid crudo, sin validar existencia, documentado así. No se tocó el motor de reglas: `attribute('COD')` ya funcionaba correctamente contra `FGetValueAttribute` real (confirmado en la investigación), esto solo agrega la administración de la configuración que consume.
- [x] CRUD de configuración del flujo/wizard (`SProcessFlow`, `SStep`, `SScreen`, `SFlowStep`) en `reference-data-service`, mismo módulo `AttributeEngineModule`. `SFlowStep` no tiene código propio (identificado por `IdeProcessFlow`+`IdeStepCurrent`+`IndResultOK`+`IdeStepForward`) así que no usa `CatalogCrudService`, se escribe a mano. **Sigue pendiente, deliberadamente afuera de esto**: la EJECUCIÓN del flujo en una sesión de cotización concreta (`FPInstanceFlow`, `FGetNextFlowStep`, `TFlowStepInstance`, el templating real `replace('#', vDesShort)`) -- eso depende de que `underwriting-service` tenga el flujo real de cotización (ver ítem de reemplazar `/rules-engine/*`), construirlo antes sería sobre pantallas que no existen.
- [x] Investigación del motor de cotización real (`packages/database/scripts/investigate-quote-engine.js`, corrido contra la BD real). Hallazgos clave: (1) "vigente" en `FQuoteRiskPlan` para elegir `SPlanProductRisk` = estado inicial Y `now()` dentro de `[TstInitial, TstEnd]`. (2) `FQuoteCoverage` arma los valores por defecto de cada `TQuoteCoverage` desde las columnas `Upper*` (no `Lower*`) cuando el flag `IndFixed*` correspondiente está activo, y preselecciona (`IndSelected`) solo las obligatorias (`IndMandatory`); filtra `SCoveragePlan` por estado "Initial", mientras que `FQuoteCoverageConcept` lo hace por estado "Activo" -- inconsistencia real del original, en la práctica ambos resuelven al mismo `IdeState` porque una `SCoveragePlan` nace ya activa. (3) Confirmado un riesgo real de colisión bajo concurrencia en `FQuote('GETQUOTENUMBER',...)`: correlativo leído de la última fila y parseado con `split_part`+1 en memoria, sin lock -- ver decisión de reemplazo abajo. (4) La rama `QUOTESUMMARY` de `FQuote` tiene dos defectos de sintaxis (coma faltante en un `json_build_object`, alias de tabla pegado al nombre) idénticos en las 3 copias del esquema -- nunca llegó a ejecutarse en producción tal cual está; la función hermana `FGetQuoteSummary` (sintácticamente válida) es la referencia confiable para cuando se implemente el resumen real. (5) Ni la creación de `TQuote`/`TQuoteRisk` ni el toggle de `IndSelected` en `TQuoteRiskPlan`/`TQuoteCoverage` existen como función PL/pgSQL -- eran CRUD directo de la capa LoopBack original, así que se diseñaron de cero en vez de reverse-engineerse (con dos reglas de negocio agregadas explícitamente, ver el ítem de abajo).
- [x] Fase 1 del motor de cotización real en `underwriting-service` (reemplaza los endpoints de prueba `/rules-engine/*` que vivían en `product-rating-service`), módulo `QuotingModule` -- ver `services/underwriting-service/README.md` para el detalle completo de rutas. Cubre: crear cotización (`POST /quotes`), cotizar/re-cotizar (`POST /quotes/:id/price`, equivalente a `FQuote('QUOTEPRICING',...)`: cascada `TQuoteRiskPlan` -> `TQuoteCoverage` -> `TQuoteCoverageConcept` vía el `RulesEngineService` ya existente, sin tocarlo) y seleccionar plan/cobertura (dos reglas de negocio agregadas explícitamente, ausentes como validación en el original: un único plan seleccionado por riesgo, y una cobertura obligatoria no se puede deseleccionar). `NumQuote` ahora se genera con una secuencia real de Postgres (`ars_platform."SeqTQuoteNumber"`, `npm run db:setup-quote-sequence`) en vez de replicar el algoritmo original con riesgo de colisión -- decisión explícita del usuario ("Secuencia real de Postgres" sobre "Replicar tal cual"), mismo formato visible `<CodProducto>-<Año>-<N>` pero `N` ahora es correlativo global, no por producto. Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera de esta fase**: resumen (`QUOTESUMMARY`/`FGetQuoteSummary`) y aceptar la cotización (`FQuote_SetState`) -- ambos dependen de `TPerson`/roles (`TOMADOR`/`TITULAR`), que todavía no existen (`party-service` sigue siendo scaffold); ver ítem siguiente. Tampoco se toca la cascada de creación de CONTRATO (`FContract`, ver §4 de la especificación).
- [x] Migración de `product-rating-service` (productos, coberturas, tarifas) — CRUD de configuración y funciones de consulta completos, incluido el motor de tarifas (`FGetRateValue` cableado en las fórmulas, ver Fase 1). **Cerrado (22/09/2026)**: revisado explícitamente a pedido del usuario junto con el ítem de `underwriting-service` de abajo -- se retiraron los endpoints de prueba `/rules-engine/*` (`ProductRatingRulesEngineModule`/`RulesEngineTestController`), condición que su propio doc-comment pedía para retirarlos ("cuando exista el CRUD real de cotización/cobertura que lo use") y que ya se cumplía: `underwriting-service` tiene su propio `UnderwritingRulesEngineModule` independiente conectado a la cascada real, y `rates.module.ts`/`RateValuesService` (el endpoint real `GET /rate-values/lookup`) registran su propio `PrismaRateValueResolver` directamente, sin depender del módulo de prueba. Verificado `tsc --noEmit` limpio en `product-rating-service`; `git status --short` sin credenciales sueltas. Ver `services/product-rating-service/README.md` para el detalle.
- [x] Migración de `underwriting-service` (cotización y contratación) — el trabajo de mayor riesgo del proyecto. **Cerrado (22/09/2026)**: cotización completa (motor de atributos/reglas, personas, resumen, transición de estado), contratación completa (cascada de creación de contrato, `CANCELCONTRACT`, recibos unificados, comisiones), el bugfix de visibilidad transaccional, y el listado/recuperación de cotizaciones con retomar el wizard -- ver los `[x]` de arriba para el detalle completo de cada pieza. Quedan pendientes explícitamente documentados y diferidos con el visto bueno del usuario, no bloqueantes para dar por cerrada esta migración: pantalla de detalle de contrato, Internacionalización (i18n), y el módulo de Siniestros (movido a su alcance real en Fase 4, ver más abajo).
- [x] Investigación de personas, roles y consentimiento GDPR en party-service (`packages/database/scripts/investigate-party-service.js`, corrido contra la BD real). Hallazgos clave: (1) `TPerson`/`TAddress`/`TContactData` no tienen función PL/pgSQL propia de escritura (solo `FPerson_GetBy`, de solo lectura) -- eran CRUD directo de la capa LoopBack original, igual que `TQuote`/`TQuoteRisk`. (2) Códigos reales de `SPersonRol` confirmados: `TOMADOR`, `TITULAR`, `BENEFICIARIO`, `ASEGURADO` (antes solo nombres informales en comentarios, sin seed/constante en el repo). (3) `FContractPerson('SETQUOTE',...)` confirma exactamente la regla de `docs/01-especificacion-motor-negocio-actual.md` §4: copia `TQuotePerson` (roles TOMADOR/TITULAR) a `TContractPerson` y recién ahí pone `TPerson.IndClient=true`+`TstRelationshipStart=now()` -- nunca antes, o sea nunca en la fase de cotización. (4) Bug real de precedencia de operadores en `FConsent` (documentado y corregido, ver ítem de implementación): el WHERE real equivale a `IdeProduct IS NULL OR (IdeProduct = X AND Activo AND vigente)`, así que los consentimientos globales se devuelven siempre sin chequear estado ni vigencia.
- [x] Fase 1 de `party-service`: personas, direcciones, datos de contacto, catálogo de roles y consentimiento GDPR -- ver `services/party-service/README.md` para el detalle completo de rutas. `PersonsModule` (`TPerson`/`TAddress`/`TContactData`, con `GET /persons/lookup` equivalente a `FPerson_GetBy`, y `SPersonRol` como catálogo `CatalogCrudService`) + `ConsentModule` (`SConsent`/`TPersonConsent`, equivalente a `FConsent` con el bug de precedencia corregido -- decisión explícita del usuario). Dos reglas de negocio agregadas explícitamente (ausentes como validación en el original): una única dirección/dato de contacto principal por persona (por clase, en el caso de contacto), y aceptar el mismo consentimiento dos veces para la misma persona+cotización es idempotente. Toda persona nace `IndLead=true`/`IndClient=false`, confirmado contra `FContractPerson` real. Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera de esta fase**: brokers (`TBroker`) y el árbol de comisiones (`SCommission*`, no bloquean nada hasta `FReceipt`); y la asociación persona+rol+cotización (`TQuotePerson`), que vive del lado de underwriting-service -- ver ítem siguiente.
- [x] Investigación de `FGetQuoteSummary` y `FQuote_SetState` (`packages/database/scripts/find-legacy-function.js`, corrido contra la BD real). Hallazgos clave: (1) `FGetQuoteSummary` es la función sintácticamente válida a usar como referencia (NO la rama `QUOTESUMMARY` de `FQuote`, con los dos defectos de sintaxis ya documentados) -- arma un JSON con moneda, plan/prima seleccionados (subconsultas escalares que asumen un único plan seleccionado en TODA la cotización, no por riesgo -- limitación real del original, replicada sin "corregirla"), personas TOMADOR/TITULAR (asume como mucho una de cada una por cotización) y por riesgo: producto de riesgo, coberturas seleccionadas y atributos parseados vía `FGetParsedRiskAttribute` (función NO investigada todavía, queda pendiente). (2) `FQuote_SetState` es genérica y reutilizable tal cual: aplica `FGetState('Next', tabla, estado, códigoOperativo)` en cascada completa (`TQuote` -> todos sus `TQuoteRisk` -> todos sus `TQuoteRiskPlan` -> todas sus `TQuoteCoverage` -> todos sus `TQuoteCoverageConcept`, no solo lo seleccionado) -- exactamente el mecanismo que ya expone `StateMachineService.getNextState`. No indica qué código operativo puntual dispara la aceptación real ni la cascada de contrato que le sigue -- eso sigue sin confirmar.
- [x] `TQuotePerson` + resumen + transición de estado genérica en `underwriting-service` (`QuotesController`/`QuotesService`, ver su README para el detalle de rutas). `PUT/GET/DELETE /quotes/:id/persons` (`TQuotePerson`, sin función PL/pgSQL propia -- `TPerson`/`SPersonRol` resueltos directo por Prisma contra el Postgres compartido con `party-service`, sin llamada HTTP entre servicios; regla agregada: asignar un rol reemplaza a quien lo tuviera antes en esa cotización). `GET /quotes/:id/summary` (equivalente a `FGetQuoteSummary`, con `attributes`/`FGetParsedRiskAttribute` deliberadamente afuera -- se expone `riskAttributeValue` crudo). `POST /quotes/:id/state` (equivalente genérico a `FQuote_SetState`, `codOperative` expuesto tal cual sin fijar un endpoint "aceptar"). Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera**: la cascada de creación de contrato (`FContract`/`FContractOperation`/`FContractBilling`/`FCoverageMovement`/`FRiskCoverage`/`FContractPerson`/`FContractFilePerson`, ver §4 de la especificación) -- "el trabajo de mayor riesgo del proyecto", y la investigación de `FGetParsedRiskAttribute` -- ver ítem siguiente.
- [x] Investigación de la cascada de creación de contrato (`packages/database/scripts/investigate-contract-engine.js`, corrido contra la BD real). Confirmado el `FContract` real completo (dispatcher con 5 casos: CONTRACTNEW/CANCELCONTRACT/GETJSONBY/GETNUMBER/SETSTATE, idéntico en las 3 copias del esquema ag2ars/entity/temporal) y todas sus operaciones (`FContractOperation`, `FContractBilling`, `FCoverageMovement`, `FMovementConcept` -- 4 casos: SetRulePrime/SetNetPrime/SetCancelConcept/SetCancelPrime --, `FRiskCoverage`, `FFileRisk`, `FReceipt`/`FReceipt_GetNumber`). Hallazgos clave: (1) mismo riesgo de colisión bajo concurrencia en `FContract('GETNUMBER',...)` que el ya resuelto para `NumQuote` -- misma decisión: secuencia real de Postgres (`SeqTContractNumber`). (2) Los tipos de vigencia no anuales (`TempPack`/`TempDays`/`TempDate`) están sin implementar en el propio original (TODO `*****` en el código real), caen a `+1 año` -- se replica ese placeholder tal cual. (3) `FReceipt` determina `IdeReceiptType` (NEW/REN/SUP) con una lógica que el propio comentario del original marca como defectuosa (`*******REVISAR ESTE PROCESO ESTA MALO*********`) -- no aplica todavía porque esta fase solo cubre el caso "contrato nuevo". (4) Operativos reales confirmados: `'Contratar'` (marca la cotización como convertida, al final de CONTRACTNEW), `'Aceptado'` (precondición de la cotización), `'Activar'` (SETSTATE final sobre todo el árbol del contrato), `'Anular'` (SETSTATE de CANCELCONTRACT, deferred), `'CONTGENE'`/`'RECEGENE'` (códigos de `SOperation` para la operación inicial de generación de contrato/recibo).
- [x] Implementación de la Fase 1 de la cascada de creación de contrato (`ContractsModule`/`ContractsController`/`ContractsService` en `underwriting-service`, ver su README para el detalle completo de rutas y alcance). `POST /quotes/:ideQuote/contract` ejecuta la cascada confirmada: `TContract` → `TContractOperation('CONTGENE')` → `TContractPerson` (copia `TQuotePerson` TOMADOR/TITULAR, marca `TPerson.IndClient`) → `TContractDistributionChannel` → `TContractBilling` → `TContractFile` → `TFileRisk`/`TRiskCoverage`/`TContractRequirement` → `TCoverageMovement`/`TMovementConcept` (reutilizando `RulesEngineService.evaluateChain` con `origin: 'Contract'`, confirmado que no requiere cambios de código) → `TReceipt`/`TReceiptDetail` → SETSTATE("Activar") del árbol completo → `FQuote_SetState('Contratar')` sobre la cotización de origen. `NumContract` vía `SeqTContractNumber` (`npm run db:setup-contract-sequence`). Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera de esta primera pasada** (ver ítems siguientes para lo ya resuelto): `FReceipt('BILLFRACTION')` (stub no-operativo confirmado en el original, sin implementar), contratos colectivos reales (`TContractFilePerson` sin poblar -- no hay dato de origen a nivel de archivo en `TQuotePerson`), y `FMovementConcept('SetNetPrime',...)` (se usa `SetRulePrime`, neto = bruto). El árbol de comisiones (`TBroker`/`SCommission*`) y `FMovementConcept('SetNetPrime',...)` (neto = bruto por entonces) se resolvieron en ítems siguientes.
- [x] Implementación de `CANCELCONTRACT` (`ContractsService.cancel()`) -- confirmada línea por línea contra el `FContract`/`FCoverageMovement`/`FMovementConcept`/`FReceipt` reales. Cascada: valida contrato `Activo` y fecha de anulación dentro de su vigencia -> resuelve operación por el endoso -> por cada `TContractFile` activo: cierre en cascada (`TFileRisk` -> `TRiskCoverage`, movimiento de cierre en 0) -> conceptos de cierre en 0 (`SetCancelConcept`) -> prorrateo día a día de la devolución de prima/comisión/impuesto según `SProductEndorsement.ConditionData` (`SetCancelPrime`) -> `TContract` a `'Modificar'` -> recibo(s) de la anulación -> cascada final de estado a `'Anular'` en las 6 entidades del árbol. Corregido en el camino un bug real de no-determinismo en `PrismaStateRuleRepository.findInitialState` (sin `ORDER BY`, podía devolver un estado inicial distinto entre dos filas `IndInitialState=true` para la misma entidad tras una reclasificación) y un bug de fixture (`TContractDistributionChannel` debía nacer `Activo`, no `Borrador` -- confirmado contra `FReceipt`/`FContract('SETSTATE',...)` reales). **Endurecida con seguridad transaccional**: toda la cascada corre dentro de un único `$transaction` de Prisma (timeout extendido a 10 minutos porque el recorrido día a día de `setCancelPrime` toma 1-2 minutos contra el pooler remoto) -- antes de esto, un fallo a mitad de camino dejaba el contrato en la BD real en un estado a medias sin forma de revertirlo (confirmado en la práctica en esta ronda). `this.stateMachine`/`this.rulesEngine` quedan deliberadamente fuera de la transacción (solo leen catálogo que esta cascada nunca escribe). Verificado end-to-end contra la BD real por el usuario: estado, movimiento de cierre prorrateado, comisión (10%) e impuesto (16%) reales, recibo tipo `SUP`.
- [x] Unificación de la generación de recibos (`FReceipt('NEWCONTRACT'|'CANCELCONTRACT', ...)`, una sola implementación en el original para ambos casos): `generateReceipts` reemplaza tanto al `createInitialReceipt` simplificado de CONTRACTNEW (resumen de una línea, `Fee=0`) como a la versión ad-hoc que se había escrito solo para `CANCELCONTRACT`. Genera SIEMPRE una operación `RECEGENE` nueva y re-apunta a ella TODOS los movimientos en Borrador del contrato, con un detalle de `TReceiptDetail` por CADA concepto de cada movimiento (no un resumen) más el cálculo real de comisión (`SCommissionTree`/`SCommissionTable`/`SCommission` del canal principal vigente) por cada concepto `PrimaNeta` -- `TReceipt.Fee` ya no queda en 0. Corregido en el camino que la comisión por línea no se redondea individualmente (fiel al original, que solo trunca el `Fee` total). Esto exigió dos correcciones más, ambas confirmadas contra el código fuente real: (1) `ContractAge` se persiste en `1` para un contrato nuevo (antes 0) -- confirmado literal contra el `INSERT` real de `TContract` en CONTRACTNEW, no una fórmula a adivinar (la fórmula de INCREMENTO en renovaciones sigue sin confirmar, no aplica todavía); (2) `createInitialMovements` ya no fija `IdeContractOperation` al crear el movimiento inicial (queda `NULL`, confirmado contra `FCoverageMovement` real) -- antes se le asignaba `CONTGENE` directamente, lo que hubiera roto el re-apunte a `RECEGENE` de `generateReceipts`. Verificado end-to-end contra la BD real por el usuario: contrato nuevo con `ContractAge=1`, movimiento re-apuntado a la operación `RECEGENE`, recibo tipo `NEW` con desglose completo por concepto y comisión real (`Fee=10`, 10% sobre `PrimaNeta=100`, coincide con `SCommission.Percentaje` configurado).
- [x] CRUD administrativo del árbol de comisiones en `party-service` (`BrokersModule`: `TBroker`, `SCommissionTree`, `SCommissionTable`, `SCommission`) -- confirmado contra el dump completo de funciones PL/pgSQL que ninguna de las 4 tiene función propia de escritura (solo referencias de solo lectura tipo `FGetIdeDesc('TBroker', ...)`), así que se diseñó de cero como CRUD administrativo, no como réplica de lógica de negocio existente. `TBroker`/`SCommissionTree` reutilizan `CatalogCrudService`; `SCommissionTable` también pero con `findAll` filtrable por `codCommissionTree`/`codProduct` (acepta `idePlanProductRisk`/`ideCoveragePlan` como uuid crudo, tablas de unión sin código propio); `SCommission` se escribe a mano (sin `Cod`/`Des` propio, mismo estilo que `SRateValue`), con `NumMovement` como correlativo de versión por `(IdeCommissionTable, IdeProcess)` calculado automáticamente (máximo existente + 1) -- ya lo consumían en la práctica `resolveCommissionPercentage`/`generateReceipts` de `underwriting-service`, así que este módulo es lo que faltaba para dar de alta comisiones reales sin tocar la BD a mano. Verificado con `nest build` limpio, y luego end-to-end contra la BD real por el usuario: creado un `TBroker` (con `SBrokerType`/`TPerson` resueltos), un `SCommissionTree` sobre `SEED_CHANNEL`, una `SCommissionTable` sobre ese árbol + `SEED_PRODUCT`, y una `SCommission` sobre esa tabla + `SEED_PROCESS` con `Percentaje=10`/`NumMovement=1` -- las 4 relaciones encadenadas correctamente. El split de comisión por canal (`SCommissionProduct`), deliberadamente afuera de esta ronda, se resolvió en el ítem siguiente.
- [x] Investigación de `FContractDistributionChannel('SETQUOTE', ...)` (línea por línea contra el dump completo, líneas ~1136-1260, idéntica en las 3 copias del esquema) y corrección de `setContractDistributionChannel` en `underwriting-service` para que la replique de verdad. Hallazgo clave confirmado: si existe configuración `Activa` en `SCommissionProduct` para (canal de origen de la cotización, producto), se crea un `TContractDistributionChannel` por CADA fila que matchee (con su canal destino, `Percentaje` e `IndMain`) -- el original NO filtra ahí por vigencia ni por `NumMovement` (a diferencia de `SCommission`/`resolveCommissionPercentage`, que sí lo hacen), toma todas las filas activas tal cual, confirmado literal. Si no existe ninguna configuración, se preserva el fallback ya implementado (un único canal, 100%, `IndMain=true`). `NumMovement` es siempre `1` en ambos casos (literal del `INSERT` real). CRUD de `SCommissionProduct` agregado a `BrokersModule` en `party-service` (`GET/POST /commission-products`, etc.) -- **decisión explícita del usuario**: sin versionado (`update()` edita en el lugar) porque la función real no distingue "versión vigente" y dos filas Activas para el mismo (producto, canal origen, canal destino) duplicarían el split en el contrato; `codProduct`/canales no se pueden cambiar en `update()` por la misma razón que en `SCommission` (para eso se crea una fila nueva). Verificado con `nest build` limpio en ambos servicios (pendiente de probarlo end-to-end contra la BD real, igual que el resto de este módulo).
- [x] Investigación e implementación de `FMovementConcept('SetNetPrime', ...)` en `underwriting-service`, confirmado línea por línea contra el código fuente real (idéntico en las 3 copias del esquema). Calcula `TMovementConcept.ConceptNetValue` de los movimientos en Borrador de cada `TRiskCoverage`: para el movimiento INICIAL (único caso que se ejercita hoy, ya que `FReceipt('BILLFRACTION')` -- que crearía movimientos subsiguientes -- sigue sin implementar), la misma fórmula se aplica a TODOS sus conceptos según `SProduct.IndProportionalPrime` -- proporcional a la fracción de pago + recargo (`SProductPaymentFraction.PorSurCharge`), o por días exactos del período de facturación vigente (`TContractBilling`) -- sin redondear, literal del original. También se implementó (código listo, no ejercitado todavía) el caso de movimientos subsiguientes: diferencia prorrateada día a día si la ventana nueva cae dentro de la anterior, o recálculo con la misma fórmula si sigue justo después dentro del período vigente; cualquier otro escenario queda sin tocar (hueco real del original, no inventado). Al terminar, `TCoverageMovement.Prime` se fija con el `ConceptNetValue` de `PrimaTotal`, y `TRiskCoverage.Prime` con el `ConceptValue` BRUTO (no neto) del último movimiento procesado -- confirmado literal, mismo patrón ya visto en `setCancelPrime`/`setCancelConcept`. `createInitialMovements` ya no deja `ConceptNetValue` igual al bruto como valor final (queda como provisorio hasta que `setNetPrime` corre a continuación en la cascada de `create()`). Verificado end-to-end contra la BD real por el usuario: `PrimaTotal` bruto `116` -> neto `9.6666...` (=116/12, fracción mensual `SEED_MENSUAL`) -> `TCoverageMovement.Prime` redondeado a `9.67`; mismo criterio en `PrimaNeta`/`Impuesto` (100/12, 16/12); `TRiskCoverage.Prime` quedó en `116` (el bruto, confirmando que ese campo no se actualiza con el neto -- comportamiento real, no bug). En el camino se encontró y corrigió un bug de fixture real: `SEED_PRODUCT` (creado por `seed-example-rules.js`) nacía sin `TstEnd`, y el `now() between pro.TstInitial and pro.TstEnd` real da NULL/falso con `TstEnd` NULL -- `setNetPrime` terminaba sin hacer nada, dejando `Prime` en el bruto. Corregido `seed-example-rules.js` para que un producto nuevo nazca con `TstEnd: FAR_FUTURE` (mismo criterio que `SPlanProductRisk` ya usaba), con un script aparte (`fix-seed-product-tstend.js`) para poner al día la fila ya existente.
- [x] Primer módulo de negocio real de `billing-service` (`BillingModule`, solo lectura). Antes de implementarlo se encontró que buena parte del alcance original del README de este servicio ya había quedado cubierta en otro lado: `underwriting-service` ya genera `TReceipt`/`TReceiptDetail` reales (con comisión) dentro de la cascada transaccional de creación/cancelación de contrato -- moverlo acá rompería esa atomicidad, así que se queda donde está --, y el CRUD del árbol de comisiones ya se hizo en `party-service` (`BrokersModule`). `TContractBilling` tampoco es un catálogo (se autogenera por contrato). **Decisión explícita del usuario**: en vez de duplicar o migrar esa lógica, `billing-service` expone `GET /receipts`/`GET /receipts/:id` (con desglose completo por concepto) y `GET /contract-billing`/`GET /contract-billing/:id`, consultando lo que `underwriting-service` ya escribe contra el mismo Postgres compartido. El reemplazo real de `ag2paymentgateway` (cobranza/pago de un recibo) queda deliberadamente afuera: no hay tabla de pagos sobre `TReceipt` en el esquema ni es una de las 44 funciones PL/pgSQL investigadas -- sería diseño nuevo, no una réplica, así que espera a una necesidad concreta. Verificado con `tsc --noEmit` y `nest build` limpios.
- [x] Migración del resto de `reference-data-service` (`CommonCatalogsModule`, `I18nModule`, `SetupModule` -- ver su README para el detalle completo de rutas). `CommonCatalogsModule`: ocho catálogos simples (`CatalogCrudService`) más `SCountry`/`SLocation`; esta última escrita a mano porque su unicidad real es compuesta (`CodLocation`+`IdeCountry`, no `CodLocation` solo), a diferencia de `SSiteMap` que sí es único global y reutiliza `CatalogCrudService`. `I18nModule`: confirmado por grep sobre el dump completo de funciones PL/pgSQL (sin resultados para `Translat*`/`GetText*` propias, solo el `pg_catalog.translate` nativo de Postgres) que `STextContent`/`STranslator` no tienen función de negocio que replicar -- CRUD puro, único real por (`IdeTextContent`, `IdeLanguage`) validado antes de insertar. `SetupModule`: `SApplication`/`SApplicationRole`/`SSiteMap` (catálogos) más `SiteMapMenuService`, migración línea por línea de `FGetSiteMap` real (confirmado idéntico en los esquemas `ag2ars`/`entity`/`temporal`; `ag2servicio.FGetSiteMap` es un sistema legado no relacionado con tablas `ARS_APLICACION_MENU*` propias, descartado) -- árbol de 3 niveles filtrado por `SSiteMapRole`/`SApplicationRole` contra una lista de códigos de rol separados por coma, ordenado por `NumOrder`; el SQL dinámico sin parametrizar del original para la lista de roles (riesgo de inyección, ya señalado en la Fase 0) se reemplazó por consultas Prisma parametrizadas. **Desviación deliberada, con visto bueno explícito del usuario**: la función original no tiene `DISTINCT`, así que un usuario con roles que comparten acceso al mismo ítem lo recibe duplicado en el original -- acá se deduplica (consecuencia natural de consultar `SSiteMap` vía Prisma, sin código extra), a diferencia del criterio usado en lógica financiera/de negocio crítica (ej. cálculo de prima), porque el árbol de navegación no lo es. `SSiteMapRole` (tabla puente rol→ítem, única real por `IdeSiteMap`+`IdeApplicationRole`) se escribió a mano, sin `update` (una concesión de acceso se otorga o se retira, no se edita). Verificado con `tsc --noEmit` y `nest build` limpios.
- [x] `gateway` real: `ProxyModule`, proxy simple 1:1 por servicio (`/iam/*`, `/party/*`, `/reference-data/*`, `/product-rating/*`, `/underwriting/*`, `/claims/*`, `/billing/*`), sin agregación -- ver `services/gateway/README.md` para el detalle completo. Alcance decidido explícitamente con el usuario: no hay comportamiento legado que replicar (confirmado en `docs/00-arquitectura.md` que el sistema v1 nunca tuvo un gateway común real -- los frontends le pegaban directo a cada servicio), así que se optó por el proxy simple en vez de adivinar la forma de endpoints agregados que ningún frontend real todavía pide. Reenvía método/query/body/headers (incluido `Authorization`) con el `fetch` global de Node 20, sin agregar `axios` como dependencia. El guard JWT global protege todo por defecto; público solo lo que cada servicio real también expone público (`GET /health` de los 7, y `auth/login`/`auth/forgot-password`/`auth/reset-password` de `iam`, sin los cuales no se podría obtener un token). Errores del downstream se propagan tal cual (status+body reales); `503` con mensaje claro si el servicio de destino no responde. Verificado end-to-end (no solo `tsc`/`nest build`) con dos servidores mock y el gateway real corriendo juntos: rutas públicas/protegidas, reenvío de `Authorization`, prefijo con guion, query string, body JSON, propagación de error `4xx` real y `503` por servicio caído -- los 10 casos probados dieron el resultado esperado. **Deliberadamente afuera**: agregación real de varios servicios en una respuesta (se decide con una pantalla real de alguno de los dos frontends) y upload de archivos (ningún servicio lo expone todavía).
- [x] Primera pasada del frontend nuevo del backoffice -- ver `apps/backoffice/README.md`. Se investigó primero el backoffice v1 real (`Code/Front/ag2backofficewebapp`, repo aparte, Angular 16 + PrimeNG, tema `lara-light-purple`, componente `ag2-table` genérico ligado a la máquina de estados, menú lateral armado contra `FGetSiteMap`) para entender qué había antes de rehacerlo. Decisión explícita del usuario: identidad visual nueva de cero (no se mantiene el violeta `#6A63B8`), pero se conserva PrimeNG como librería de componentes complejos + Tailwind CSS 4 para el layout. **Pinneado a Angular 21 + PrimeNG 21 a propósito, no la última versión**: la primera pasada usó Angular 22 + PrimeNG 22, pero al probarlo en pantalla apareció un cartel real "Invalid PrimeUI License" -- PrimeNG dejó de ser MIT a partir de la v22 (licencia paga o "Community" gratuita pero a trámite, con límites de tamaño de empresa y renovación anual; confirmado contra la página oficial de PrimeTek, `primeui.dev/nextchapter`: "v21 y anteriores quedan MIT para siempre"). Se bajó toda la app a v21 para no depender de la licencia de un tercero -- bonus, Angular 21 pide Node 22.12+ en vez de 22.22.3+, así que de paso se pudo sacar el paso extra de `nvm` que se había agregado antes. Esqueleto real en `apps/backoffice` (Angular 21 standalone, sin NgModules) -- login de dos pasos contra `iam-service` real vía el `gateway` (incluye el 2FA recién implementado), guard + interceptor JWT, layout con sidebar dinámico contra `GET /reference-data/site-map-menu` (real, no simulado) y dashboard placeholder. **Verificado en pantalla por el usuario, de punta a punta**: `npm install`, login real, entrada al dashboard, y confirmado que el cartel de licencia de PrimeNG ya no aparece con v21. En el camino se corrigieron varios errores reales de API de PrimeNG 22 (luego irrelevantes al bajar a v21, pero el patrón usado -- content projection para label/icon de `pButton` en vez de inputs -- sigue siendo válido) y un bug real de orden de inicialización en Angular (`TS2729`) encontrado recién al poder correr el compilador (`ngc`) una vez el usuario instaló las dependencias. Próximo módulo de negocio decidido: Catálogos/diccionario (`reference-data-service`).
- [x] Primer módulo de negocio real del frontend nuevo: pantalla de Catálogos comunes (`apps/backoffice/src/app/features/catalogs`) -- decidido explícitamente con el usuario (entre `CommonCatalogsModule`, `FieldCatalogModule` y `SetupModule`, los tres candidatos razonables detrás de "Catálogos/diccionario"). Un solo componente genérico (`CatalogsComponent`, configurado por `CATALOG_REGISTRY`) reutilizado para los 8 catálogos "simples" (idiomas, géneros, estado civil, profesiones, actividad económica, tipos de identificación, parentescos, tipos de contacto) más `SCountry` (con su idioma por defecto como `p-select`) -- alta, edición, activar/inactivar con confirmación. **Cambio de contrato acompañante en el backend**, aditivo y sin romper nada existente: se agregó `include: { SState: true }` (y `SLanguage: true` para `SCountry`) a los `CatalogCrudService` de `CommonCatalogsModule` -- sin esto, `findAll`/`create`/`update`/`setState` devolvían `IdeState` como UUID crudo, sin forma de que el frontend supiera si una fila estaba "ACTIVO" o no (mismo patrón de `include` que ya usaba `application-roles.service.ts` con `SApplication`, solo que ningún catálogo común lo tenía todavía). `SLocation` queda deliberadamente afuera de este registro -- es jerárquico y su unicidad es compuesta (`CodLocation`+`IdeCountry`), amerita pantalla propia (hecha en el siguiente punto). Verificado con `tsc --noEmit` (backend) y `ngc --noEmit` (frontend) limpios.
- [x] Pantalla de Ubicaciones (`apps/backoffice/src/app/features/locations`), cierre del pendiente dejado en el punto anterior. `SLocation` no encaja en el `CatalogsComponent` genérico (unicidad compuesta `CodLocation`+`IdeCountry`, jerarquía propia vía `codLocationParent`), así que se hizo una pantalla dedicada con navegación por breadcrumb: cada nivel pide solo sus hijos directos contra el mismo filtro que ya expone `locations.service.ts` (`GET /locations?codLocationParent=<cod o ''>`), sin armar un árbol completo del lado del cliente. Filtro opcional por país, alta/edición/activar-inactivar. **Deliberadamente afuera todavía**: reasignar país o mover una ubicación a otro padre -- el backend soporta lo segundo (`UpdateLocationDto.codLocationParent`), pero un selector de "ubicación padre" con búsqueda entre potencialmente miles de filas es una pantalla aparte, no se inventó ahora. Verificado con `ngc --noEmit` limpio.
- [x] Pantalla de Configuración de menú (`apps/backoffice/src/app/features/menu-config`), cierre del pendiente dejado en el punto anterior (el usuario había observado que el sidebar dinámico no tenía datos, y pidió explícitamente seguir con esto una vez cerrada Ubicaciones). Cubre las 4 entidades de `SetupModule`: `SApplication`, `SApplicationRole` (con `codApplication` como FK obligatoria e inmutable después de creado, mismo patrón que el país en Ubicaciones), `SSiteMap` (árbol, mostrado con `p-treeTable` de PrimeNG en vez de breadcrumb -- se pidió explícitamente ver la jerarquía completa de un vistazo) y `SSiteMapRole` (matriz de checkboxes: un rol de aplicación a la vez, todos los ítems de menú como filas -- también decisión explícita del usuario, en vez de una tabla plana de concesiones con diálogo). Las 4 se agruparon en una sola ruta con pestañas (`p-tabs` de PrimeNG 21) en lugar de 4 rutas separadas -- decisión explícita del usuario, por estar fuertemente relacionadas entre sí (un rol pertenece a una app, un permiso conecta un rol con un ítem de menú). **Resguardo de UI, no del backend**: `FGetSiteMap`/`SiteMapMenuService` recorre como máximo 3 niveles reales (confirmado leyendo el código: la recursión corta con `level < 3` sobre el nivel del ítem actual); un ítem de nivel 4 quedaría guardado pero jamás se mostraría en ningún menú real. El backend no impone ese límite al crear/editar `SSiteMap` (no hace falta: nada lo pedía), así que la pantalla lo hace de su lado -- solo ofrece como "padre" ítems de nivel 1 o 2. **Cambio de contrato acompañante en el backend**, mismo patrón aditivo que en Catálogos comunes: se agregó `include: { SState: true }` a `applications.service.ts` y `site-map.service.ts` (antes sin include), y se extendió el de `application-roles.service.ts` de `{ SApplication: true }` a `{ SApplication: true, SState: true }`. De paso se corrigió un bug real reportado por el usuario, no relacionado con este módulo: `services/reference-data-service/.env` (gitignored) tenía `PORT=3005` en vez de `3004` (copy-paste de `underwriting-service/.env`), causando el mismatch de puertos que veía contra el `gateway`. **Dos bugs reales más, encontrados al probar la pantalla en pantalla con el usuario**: (1) el usuario reportó aplicaciones "creadas inactivas" y que activarlas no parecía reflejarse -- no era un bug de código: tenía `reference-data-service` corriendo con `npm run start:reference-data`, que compila una vez y corre el `dist/main.js` ya compilado (a diferencia de `:watch`), así que el proceso viejo todavía no tenía el `include: { SState: true }` agregado en este mismo punto -- se resolvió reiniciando el servicio, no hizo falta tocar código. (2) el selector de aplicación al crear un rol (y, por el mismo motivo, el de rol de aplicación en la pestaña de Permisos) podía salir vacío incluso con el servicio ya reiniciado: como las 4 pestañas de `MenuConfigComponent` se montan todas juntas, cada una consulta su lista de apoyo (aplicaciones, roles, ítems de menú) una sola vez en el `constructor` -- si el usuario creaba datos nuevos en una pestaña y volvía a otra sin recargar la página entera, esa lista quedaba desactualizada. Se corrigió re-consultando esas listas de apoyo justo antes de que se vuelvan a necesitar: al abrir "Nuevo rol" (`application-roles-tab.component.ts`) y al abrir el desplegable de rol o cambiar de rol seleccionado en Permisos (`site-map-roles-tab.component.ts`, vía `(onShow)` de `p-select` y en `onRoleChange`) -- en vez de, por ejemplo, forzar recarga de toda la página o acoplar las 4 pestañas entre sí. Verificado con `tsc --noEmit` (backend) y `ngc --noEmit` (frontend) limpios.
- [x] Sidebar migrado a 100% dinámico + script de seed para las 4 pantallas base -- pedido explícito del usuario, una vez probada en pantalla la pantalla de Configuración de menú ("podrías generarme el script para el sitemap completo, ya que ahora mismo no tenemos todas las pantallas y rutas definidas"). Antes el sidebar tenía 4 links fijos hardcodeados (Inicio/Catálogos/Ubicaciones/Configuración de menú) MÁS la sección dinámica de `SSiteMap` aparte; si se cargaban esas mismas 4 pantallas en `SSiteMap` iban a quedar duplicadas. Se le presentó la disyuntiva al usuario (mantener el hardcodeo y aceptar duplicados, o migrar todo a dinámico) y eligió migrar todo -- se sacaron los 4 links fijos de `sidebar.component.html`, que ahora renderiza únicamente desde `GET /site-map-menu`. `packages/database/scripts/seed-menu-config.js` (registrado como `npm run db:seed-menu-config` en la raíz y en `packages/database/package.json`, mismo patrón que `db:seed-admin-user`/`db:seed-example-rules`: script Node plano contra `@prisma/client`, idempotente por código único) crea `SApplication` "BACKOFFICE", `SApplicationRole` "ADMIN" y los 4 `SSiteMap` raíz (código/ruta/ícono calcados de `app.routes.ts`), y concede los 4 a "ADMIN" vía `SSiteMapRole`. **Decisión no trivial, confirmada con el código real en vez de asumida**: el código de `SApplicationRole` se hizo literalmente "ADMIN" a propósito -- se confirmó leyendo `sidebar.component.ts` (`this.siteMap.getMenu(role)` con `role` = claim `role` del JWT) y `jwt-payload.interface.ts` (ese claim es el `CodRol` real de `iam-service`, `TRol`) que `SiteMapMenuService.getMenu` filtra `SApplicationRole.CodApplicationRole` contra ese mismo código, y que `db:seed-admin-user.js` ya crea el `TRol` real con `CodRol="ADMIN"` -- si no coinciden literalmente, el usuario admin no vería nada. **No se pudo correr el script desde este entorno de trabajo**: el bridge a la máquina del usuario ejecuta en una VM Linux aparte (mismo filesystem, arquitectura distinta), y el `@prisma/client` del repo está generado para `darwin-arm64` -- typecheck y ediciones de texto no lo necesitan pero el motor de consultas de Prisma sí, así que el usuario lo corre en su propia terminal. Verificado con `ngc --noEmit` (frontend) limpio; el script en sí queda pendiente de que el usuario lo corra y confirme el resultado contra Postgres real.
- **Corregido (22/09/2026): este ítem en realidad es el alcance completo de Fase 4, no un pendiente menor de Fase 2 -- movido/detallado ahí.** Investigado al considerar arrancarlo: en el backoffice NUEVO no existe absolutamente nada de Siniestros (ni ruta, ni componente, ni entrada de menú -- confirmado, no hay ni rastro en `app.routes.ts`/`features/`). El "cascarón vacío" que describía este ítem en realidad es del **backoffice v1 (el viejo)** -- así lo dice el propio `services/claims-service/README.md` ("en el backoffice v1 el módulo de siniestros existe solo como cascarón, routing sin componentes"). Backend: `services/claims-service` es un scaffold sin lógica de negocio (solo `/health`), y a diferencia de cotización/contratación, NO hay ninguna familia `FClaim*` identificada/extraída del legado todavía (`grep -rniI "FClaim" docs/ packages/database` sin resultados) -- territorio sin investigar. Sí existe el modelo de datos real en Postgres: `SClaimType`/`SClaimEvent` (catálogo) + `TClaim`/`TClaimFile`/`TClaimOperation`/`TClaimRequirement`/`TClaimRisk` (transaccionales), con relaciones reales a contrato/cobertura -- 7 tablas, tamaño moderado. Decisión explícita del usuario: no arrancar esto ahora (evita repetir con Siniestros el mismo salto de fase que ya se evitó con consentimiento+pago, ver el ítem de Fase 5 sobre eso) -- ver el detalle completo movido a Fase 4 más abajo.
- [x] Doble factor de autenticación (TOTP) en `iam-service`, opcional y autoservicio -- ver su README para el detalle completo de rutas. Feature nueva de cero (v1 tampoco la tenía, confirmado), así que sin comportamiento legado que replicar -- diseño decidido explícitamente con el usuario: (1) solo TOTP, sin respaldo por email; (2) opcional autoservicio, no obligatorio para ningún rol; (3) el secreto TOTP se guarda en texto plano (no se puede hashear, y cifrarlo sumaría gestión de una clave de aplicación nueva fuera de alcance ahora). TOTP (RFC 6238 sobre HOTP/RFC 4226) implementado a mano con el módulo `crypto` de Node -- sin librería nueva (`otplib` o similar) porque este entorno de trabajo no puede instalar paquetes ahora mismo (política de red bloquea npm y binaries.prisma.sh, ver más abajo) -- **verificado línea por línea contra los vectores de prueba oficiales de la RFC** antes de integrarlo (no solo `tsc`/`nest build`). Login de dos pasos: `POST /auth/login` devuelve `{requiresTwoFactor, twoFactorToken}` (token de 5 minutos, mismo patrón `purpose` ya usado en recuperación de contraseña) en vez del JWT cuando el usuario tiene 2FA activo; `POST /auth/2fa/verify` completa el segundo paso con un código TOTP o uno de los 10 códigos de respaldo de un solo uso (hasheados con bcrypt). Apagar 2FA exige reingresar la contraseña Y un código válido, no solo estar logueado. **Primera evolución real del esquema en Fase 2** (`TUserTwoFactor`/`TUserBackupCode`, `db/migrations/002_two_factor_auth.sql`): decisión explícita del usuario de seguir con el script SQL simple (mismo patrón de la migración 001) en vez de adoptar Prisma Migrate como planeaba originalmente `db/README.md` -- la BD real es un Postgres remoto compartido (mismo servidor de Vercel que usa `entity`/v1) y `prisma migrate dev` necesita una shadow database ahí, con permisos inciertos; no valía el riesgo para una feature chica. **Limitación técnica superada**: durante el desarrollo inicial no se pudo correr `prisma generate` en el entorno de trabajo de ese momento (la descarga del motor de consultas estaba bloqueada por la política de red, igual que el registro de npm), así que `TUserTwoFactor`/`TUserBackupCode` se accedían con `$queryRaw`/`$executeRaw` parametrizado. El usuario corrió `npx prisma generate` en su propia terminal después de aplicar la migración (y corrigió de paso un error real de sintaxis en `schema.prisma`: los comentarios de bloque `/** */` no son válidos en el DSL de Prisma, solo `//`/`///`), y `TwoFactorService` se refactorizó al cliente Prisma tipado, igual que el resto del proyecto. **Sin cobertura todavía**: recuperación de cuenta si se pierden el celular y los 10 códigos de respaldo a la vez (soporte manual, fuera de alcance de esta primera versión).
- [x] "Catálogos de producto" en `apps/backoffice`: pantallas de administración para toda la jerarquía de producto, tarifas y distribución que hacía falta antes de poder construir la pantalla de Cotización (que ya tiene backend completo en `underwriting-service` desde la fase anterior, pero sin ningún catálogo administrable para elegir `codProduct`/`codDistributionChannel`/`codDistributionWay`/`codRiskProduct`). Dos decisiones explícitas del usuario (`AskUserQuestion`): **alcance completo de una sola vez** (los 8 catálogos simples de tarifación + la jerarquía entera de producto + canal/vía de distribución + tablas de tarifa, no un subconjunto mínimo para probar Cotización), y **Catálogos aparte + Productos maestro-detalle** como estructura de UI (una pantalla de "Catálogos" con tabs planos para las entidades independientes, y una pantalla "Productos" aparte con navegación maestro-detalle para la jerarquía dependiente de un producto), descartando tanto una mega-página de tabs como un wizard paso a paso. **Reducción de alcance real, descubierta en el camino**: ya existía un componente de catálogo genérico (`CatalogsComponent`/`CATALOG_REGISTRY`) de la fase de Catálogos comunes, así que las 13 entidades independientes (`risk-levels`, `risks`, `risk-types`, `currencies`, `insurance-areas`, `insurance-lines`, `deductible-types`, `limit-types`, `coverages` de `product-rating-service`; `channel-types`, `distribution-ways`, `distribution-channels` de `party-service`; `concept-types`, `concepts` de `reference-data-service`) se resolvieron solo agregando entradas al registro, sin componentes Angular nuevos. Lo que sí exigió componentes nuevos fue lo genuinamente jerárquico: **Productos** (`features/products`) -- maestro-detalle real sobre `SProduct`, con 5 subpestañas ligadas al producto elegido (`input.required<CatalogRow>()`, primer uso de Angular signal inputs en esta app): `SRiskProduct`, `SPlanProduct`, `SPlanProductRisk`, `SCoveragePlan` (~23 campos: deducible, límite, rangos de monto/tasa/prima, obligatoriedad, período de carencia) y `SCalculationRule` (editor de la fórmula `IF`/`THEN`/`ELSE` del mismo `RulesEngineService`, sobre el JSON `FormulaJSON` confirmado contra `calculation-rules.service.ts` real) -- y **Tablas de tarifa** (`features/rate-tables`) para `SRateTable`/`SRateFactor`/`SRateValue`. **Dos módulos de backend nuevos, descubiertos durante esta fase y no parte del pedido original**: `SChannelType`/`SDistributionChannel`/`SDistributionWay` no tenían ningún CRUD en ningún servicio (el propio comentario de `brokers.service.ts` los daba por "asumidos sembrados") -- se agregó `DistributionModule` en `party-service` (junto a `BrokersModule`, porque `SDistributionChannel` resuelve FKs opcionales a `TBroker`/`TPerson` que ya viven ahí, sin llamada HTTP entre servicios); y `SConcept`/`SConceptType` -- requeridos por `SCalculationRule.CodConcept` (obligatorio) y sin CRUD en ningún lado -- se agregaron a `CommonCatalogsModule` de `reference-data-service` (reference data genuinamente transversal, ya consumida también por `billing-service` y `underwriting-service`). Dos `include` aditivos más, mismo patrón que en fases anteriores (no rompen el contrato existente): `SRiskLevel` en `risks.service.ts` y `SInsuranceArea` en `insurance-lines.service.ts` de `product-rating-service`. `packages/database/scripts/seed-menu-config.js` extendido con `PRODUCTOS`/`TARIFAS` como ítems raíz nuevos. Verificado con `tsc --noEmit` en los tres servicios tocados (`party-service`, `product-rating-service`, `reference-data-service`) y `ngc --noEmit` en `apps/backoffice`, todos limpios. **Segunda ronda, pedida por el usuario al probar la pantalla en pantalla**: la de "Catálogos" había quedado desorganizada con los 23 catálogos (9 comunes + 14 de producto) mezclados en una sola lista. Se dividió `CATALOG_REGISTRY` en `COMMON_CATALOG_REGISTRY` (los 9 de siempre, se queda en "Catálogos") y `PRODUCT_CATALOG_REGISTRY` (los 14 de producto, pantalla nueva "Catálogos de producto") -- mismo componente `CatalogsComponent`, ahora genérico: qué registro/título/subtítulo usar sale de `route.data` (leído vía `ActivatedRoute` en el constructor, sin sumar `withComponentInputBinding` solo para esto). Presentadas 3 alternativas de agrupación (`AskUserQuestion`), eligió la recomendada: **ítem padre "Configuración de productos" con 3 hijos en el sidebar** (Catálogos de producto, Productos, Tablas de tarifa) -- reutiliza el árbol de hasta 3 niveles que `SSiteMap`/`SiteMapMenuService.buildLevel` ya soportaban desde "Configuración de menú", sin backend nuevo. Confirmado contra el código real que el acceso de `SSiteMapRole` NO se hereda del padre a los hijos (`buildLevel` filtra por rol en cada nivel), así que el padre necesita su propia concesión igual que cada hijo. Rutas movidas a `/configuracion-productos/{catalogos,productos,tarifas}` (`app.routes.ts`, ahora con rutas hijas anidadas). `seed-menu-config.js` pasó de "crear si falta" a upsert real (`update` si el código ya existía) -- necesario para poder reasignar `PRODUCTOS`/`TARIFAS` de ítems raíz a hijos de `CONFIG_PRODUCTOS` sin duplicar filas ni dejar la jerarquía vieja pisada a mitad de camino. Verificado con `ngc --noEmit` en `apps/backoffice`, limpio; pendiente de que el usuario corra `npm run db:seed-menu-config` de nuevo para que el sidebar quede reorganizado.
- [x] Internacionalización (i18n) del frontend de `apps/backoffice`: extraídos todos los textos de UI hardcodeados en español (templates Y config/TS/mensajes de toast/confirm) a un mecanismo de traducción real, dejando la app funcionando 100% en español pero con el mecanismo listo para agregar más idiomas después -- sin tocar `STextContent`/`STranslator` (`I18nModule` de `reference-data-service`), que es traducción de CONTENIDO de negocio (ej. la descripción de un producto en varios idiomas), no de los textos fijos de la interfaz; distinción reconfirmada explícitamente con el usuario antes de arrancar. Tres decisiones explícitas del usuario (`AskUserQuestion`): **librería** -- `@jsverse/transloco` (recomendada sobre `ngx-translate`, menos mantenido, y sobre `@angular/localize` nativo de Angular, que exige build separado por idioma; Transloco es un solo build con archivos JSON en runtime, que era además la preferencia explícita del usuario -- "me gusta la idea del manejo de ficheros json"); **alcance de idiomas** -- solo español por ahora, dejando el mecanismo listo; **selector visible** -- solo infraestructura por ahora, sin selector de idioma en la UI todavía. **Bloqueo de red superado**: `npm install` del paquete nuevo devolvía `403 Forbidden` de `registry.npmjs.org` tanto desde el bridge a la máquina del usuario como desde este entorno de trabajo (confirmado que es una política de red real, no un proxy mal configurado -- el propio proxy tenía a `registry.npmjs.org` en su lista de bypass, así que el pedido iba directo y aun así lo rechazaban) -- el usuario instaló `@jsverse/transloco@8.4.0` en su propia terminal. Arquitectura: `TranslocoHttpLoader` (`core/i18n/transloco-http-loader.ts`) sirve `apps/backoffice/public/i18n/es.json` como asset estático (mismo mecanismo que ya usa `public/brand/logo.png`); diccionario único de ~430 claves en 15 namespaces (uno por pantalla/área -- `shell`, `header`, `sidebar`, `login`, `dashboard`, `catalogsRegistry`, `catalogFields`, `common`, `catalogs`, `locations`, `menuConfig`, `products`, `quotes`, `quotesList`, `rateTables`), con namespaces genéricos (`common.*`, `catalogs.*`) reutilizados entre pantallas para texto CRUD idéntico (ej. "Activar"/"Guardar"/mensajes de alta-edición) sin duplicar entradas. **Segunda decisión explícita del usuario, tras empezar la extracción de Catálogos**: alcance completo incluyendo config/TS (`catalog.model.ts`: nombres de catálogo y etiquetas de campo, que viven en TypeScript, no en templates) y mensajes de toast/confirm, no solo texto estático de templates. Deliberadamente FUERA de esta extracción (no son texto de UI hardcodeado, son datos): las etiquetas dinámicas del motor de atributos de riesgo (`field.label`/`opt.key` en `quotes.component.html`, provienen de `SFieldDictionary`/`RiskAttributeField`, configurables en BD) y `node.Titulo` del sidebar (viene de `SSiteMap` real vía `GET /site-map-menu`) -- mismo criterio en ambos casos. **Bug de TypeScript encontrado y su fix, aplicado en todos los archivos**: `TranslocoService.translate<T = string>()` sin argumento de tipo explícito, usado como operando derecho de `??`, a veces infiere `T` del tipo contextual de la expresión completa en vez de `string` por defecto, rompiendo la asignación a propiedades tipadas `string` (ej. `ToastMessageOptions.detail`) -- se resolvió llamando siempre `this.transloco.translate<string>(...)` con el argumento de tipo explícito en ese contexto. **Preservación fiel de variantes gramaticales del texto original**, sin "corregir" nada: `applications-tab.component.ts` usa "activa"/"inactiva" (concuerda con "aplicación", femenino) mientras el resto de las pantallas usa "activo"/"inactivo" -- se agregaron `common.activeFem`/`common.inactiveFem` en vez de unificar. Se extrajeron también los ~24 componentes restantes de `apps/backoffice` de punta a punta en una sola pasada (decisión explícita del usuario, `AskUserQuestion`: seguir derecho con todo en vez de pausar a probar entre pantallas), incluyendo la pantalla de Cotización completa (`quotes.component`, la más grande: 5 pasos del wizard) y su listado (`quotes-list.component`), y Tablas de tarifa (`rate-tables.component`). Verificado con `ngc --noEmit` limpio después de cada archivo/lote, sin una sola corrupción de texto en el proceso (edición vía scripts Python con `assert count == N` antes de cada reemplazo, para garantizar unicidad exacta del texto a reemplazar). Pendiente para cuando se agregue un segundo idioma real: agregar el selector de idioma a la UI (`header`/`shell`) y decidir qué otros idiomas ofrecer -- ninguna decisión tomada todavía al respecto.
- [x] Pantalla de Cotización en `apps/backoffice` (`features/quotes`), Etapa 1 de varias (decisión explícita del usuario, `AskUserQuestion`: por etapas en vez de todo el wizard de una vez) -- lo mínimo para ver un precio real en pantalla: crear una cotización (`POST /quotes` de `underwriting-service`, con producto + canal/vía de distribución + al menos un riesgo), cotizarla (`POST /quotes/:id/price`, equivalente real a `FQuote('QUOTEPRICING',...)`, ya implementado desde la fase anterior) y elegir un plan por riesgo (`PATCH .../plans/:id/select`) -- cada plan muestra su precio real y sus coberturas obligatorias/opcionales con `DesShort`/`DesLarge` (el trabajo reciente de esos campos resultó ser un prerequisito real, no cosmético, para esta pantalla). Ruta nueva `/cotizacion`, agregada como segundo ítem raíz del sidebar (después de Inicio) en `seed-menu-config.js`. Deliberadamente afuera de esta etapa (quedan para las siguientes): seleccionar/deseleccionar coberturas opcionales, asociar personas, resumen y aceptar/contratar. Verificado con `ngc --noEmit` limpio; probado por el usuario contra `underwriting-service` real -- confirmado funcionando.
- [x] Cotización, Etapa 1.1: seleccionar/deseleccionar coberturas opcionales sobre el plan ya elegido (decisión explícita del usuario, `AskUserQuestion`: de a una, empezando por esta pieza). Usa el endpoint que ya existía y se había confirmado durante la investigación de la Etapa 1 (`PATCH :id/risk-plans/:ideQuoteRiskPlan/coverages/:ideQuoteCoverage`, body `{ selected: boolean }`, `ToggleCoverageDto` real) -- no hizo falta ningún cambio de backend. En `quotes.component.html`, el bloque de "Coberturas opcionales" pasó de texto de solo lectura a un `p-checkbox` (`primeng/checkbox`, modo `binary`, confirmado contra su `.d.ts` real antes de usarlo) por cobertura, que dispara `toggleCoverage()` y refresca el precio con la respuesta del backend. Las coberturas obligatorias ("Coberturas incluidas") se dejaron intencionalmente de solo lectura, sin checkbox: el backend responde 409 (`ConflictException`) si se intenta deseleccionar una obligatoria, así que el frontend ni siquiera ofrece esa opción. Verificado con `ngc --noEmit` limpio; probado por el usuario contra `underwriting-service` real (incluyendo un caso de datos: al principio no aparecía ninguna cobertura opcional porque el plan probado no tenía ninguna `SCoveragePlan` con `Obligatoria = No` cargada en catálogo -- no era un bug, se confirmó creando una cobertura opcional en Catálogos → Productos → "Coberturas del plan" y probando de nuevo) -- confirmado funcionando. Quedan para las siguientes etapas (de a una, según lo acordado): asociar personas, resumen y aceptar/contratar.
- [x] Cotización, Etapas 1.2 a 1.5: el usuario pidió cerrar de una vez el resto del flujo (personas, resumen, aceptar/contratar) para poder pausar el desarrollo y configurar un producto real, y probar 100% de punta a punta sin más piezas por construir. Backend: ninguna investigación pendiente, todo ya estaba construido en fases anteriores (`QuotesController`/`ContractsController` reales, ver README de `underwriting-service`) -- se confirmaron línea por línea antes de tocar el frontend: `PUT/GET/DELETE /quotes/:id/persons` (`TQuotePerson`), `GET /quotes/:id/summary` (`FGetQuoteSummary`), `POST /quotes/:id/state` (`FQuote_SetState`, genérico) y `POST /quotes/:ideQuote/contract` (`ContractsService.create`, la cascada completa de `FContract('CONTRACTNEW',...)`, ya implementada). Personas: nuevo `PersonsService` (`features/quotes/persons.service.ts`) contra `party-service` (`GET /party/persons/lookup` por número de identificación, `POST /party/persons` si no existe) -- la pantalla busca o crea Tomador y Titular (los dos únicos roles que `ContractsService.setContractPersons` copia al contrato, confirmado contra su código real) y los asocia con `PUT /quotes/:id/persons`, con un atajo "usar la misma persona que el Tomador" para el Titular. Resumen: pantalla de solo lectura sobre `GET /quotes/:id/summary` (plan, prima total, Tomador/Titular, coberturas por riesgo). Aceptar/contratar: dos botones -- "Aceptar cotización" (`POST /quotes/:id/state` con `codOperative: 'Aceptar'`, código confirmado contra `ContractsService.create`, que exige literalmente ese estado) y "Generar contrato" (`POST /quotes/:id/contract`, sin `codPaymentFraction`/`initialDate` en esta etapa -- se usan los valores por defecto del backend). La pantalla completa quedó como un flujo de 5 pasos (`step` signal: form -> plans -> persons -> summary -> contract) en el mismo componente. Verificado con `ngc --noEmit` limpio; `git status --short` sin credenciales sueltas. **Probado por el usuario de punta a punta contra el backend real -- cotizó y contrató con éxito** (ver el hallazgo sobre la máquina de estados más abajo). De paso se destrabó un problema aparte, no relacionado con este código: el sidebar dinámico aparecía vacío -- resultó ser sesión/menú desactualizados (subsanado corriendo `db:seed-menu-config` y volviendo a iniciar sesión), no un bug de esta pantalla.

  **Hallazgo de esta investigación, luego corregido contra la BD real**: al confirmar el requisito de `ContractsService.create` (`getStateByCode('Aceptado')`) se había concluido que la BD real (`ars_platform`) no tenía cargada ninguna fila de `SEntity`/`SStateRule`. El usuario probó igual "Aceptar cotización"/"Generar contrato" sin recordar haber corrido ningún seed y funcionó de punta a punta -- una nueva investigación de solo lectura (`node packages/database/scripts/investigate-state-machine-config.js`, corrida por el usuario contra la BD real, mi sandbox no tiene salida de red hacia ese host) confirmó por qué: SÍ existen 20 `SEntity`/50 `SStateRule` reales, las 50 creadas por `UsrCreation='seed-script'` -- son exactamente las del fixture de prueba (`seed-contract-testing-fixtures.js`), con estados propios prefijados `SEED_` (`SEED_BORRADOR`, `SEED_CONTRATADO`, `SEED_MODIFICADO`, `SEED_ANULADO`) conviviendo con los estados reales (`ACTIVO`, `ACEPTADO`, etc.). Alguien las corrió en una sesión anterior sin dejarlo registrado.

  Dato clave para lo que sigue: `SState`/`SStateRule`/`SEntity` NO están particionados por producto (`SStateRule` no tiene `IdeProduct`) -- es una máquina de estados GLOBAL para las ~20 entidades del árbol de cotización/contrato, no algo que haya que volver a configurar por cada producto nuevo. Esto significa que la configuración de producto real que el usuario va a hacer a continuación (tarifas, coberturas, planes, etc.) NO necesita tocar la máquina de estados -- ya está resuelta, aunque sea con los códigos `SEED_` de prueba en vez de los códigos reales del legado.

  **Pendiente, sin resolver, no bloqueante**: decidir con el usuario si esta máquina de estados `SEED_` (deliberadamente simplificada, ver la cabecera de `seed-contract-testing-fixtures.js`: "no pretende replicar los estados reales del legado") queda como la configuración definitiva del sistema, o si en algún momento conviene investigar y migrar los estados/transiciones reales del legado (`SState`/`SStateRule` de la BD original, si son recuperables) para mayor fidelidad con el sistema original -- y, en cualquier caso, si conviene una pantalla de catálogo dedicada para `SEntity`/`SStateRule` en vez de cargarlos por script.
- [x] Listado/recuperación de cotizaciones: `underwriting-service` hoy solo expone `GET /quotes/:id` (una por id, confirmado contra `quotes.controller.ts` real) -- no hay forma de listar cotizaciones existentes ni de retomar una ya creada desde el backoffice. Pedido explícito del usuario al probar la Etapa 1 de Cotización. Falta: (a) un `GET /quotes` en el backend (a definir con el usuario si pagina, si filtra por estado/producto/fecha, y si hace falta un `IdeUser`/vendedor en `TQuote` para filtrar "mis cotizaciones" -- hoy `TQuote` no tiene esa columna, habría que confirmar contra el esquema real antes de asumir que se puede agregar sin romper nada) y (b) una pantalla de listado en `apps/backoffice` (tabla con buscar/abrir, en vez del flujo único actual) que reemplace o complemente la pantalla de creación de la Etapa 1.

- [x] Dos bugs de UI reportados por el usuario al empezar a configurar un producto real en "Catálogos de producto", ambos en `CatalogsComponent` (`features/catalogs/`, la pantalla genérica reutilizada por "Catálogos" y "Catálogos de producto"):
  1. **Cambiar de catálogo con un campo `select` rompía la pantalla** (`TypeError: null is not an object (evaluating 'this.ngControl.valueChanges.subscribe')`, la tabla se quedaba mostrando los datos del catálogo anterior). Causa real, confirmada leyendo el template: `<p-dialog>` solo se oculta con CSS (`[visible]="dialogVisible()"`, sin `@if`/`*ngIf` alrededor) -- su contenido, incluido el `@for` de campos extra, está SIEMPRE en el árbol de Angular y reacciona a `selected()` apenas se cambia de catálogo, sin importar si el diálogo está abierto. Como `buildForm()` solo arma `this.form` al abrir el diálogo (`openCreate`/`openEdit`), `getExtraControl(key)` devolvía `null` para cualquier catálogo con campos `select` que todavía no se hubiera editado -- `p-select` con un `[formControl]` nulo tira esa excepción y aborta el ciclo de detección de cambios a mitad de camino. Arreglado en `catalogs.component.ts`: `getExtraControl` ya no puede devolver `null` (crea un control vacío al vuelo si todavía no existe -- inofensivo, se descarta apenas se abre el diálogo de verdad); `selectCatalog()` además cierra el diálogo al cambiar de catálogo, para no dejarlo abierto apuntando a datos de otro catálogo.
  2. **El dropdown de "Tipo de canal" (y en general cualquier `p-select` dentro de un diálogo) se cortaba visualmente**, sin dejar ver/elegir las opciones de más abajo. Causa real: `p-dialog` tiene `overflow-y: auto` en su contenido, y ningún `p-select` de toda la app usaba `appendTo="body"` (confirmado con `grep`, cero resultados) -- sin eso, el panel de opciones se renderiza DENTRO del diálogo y queda recortado por ese `overflow`. Agregado `appendTo="body"` a las 24 instancias de `p-select` de toda la app (`risk-products-tab`, `calculation-rules-tab`, `products`, `coverage-plans-tab`, `plan-product-risks-tab`, `site-map-roles-tab`, `application-roles-tab`, `site-map-tab`, `quotes`, `rate-tables`, `locations`, `catalogs` -- 12 archivos), no solo en "Canal de distribución": el usuario pidió explícitamente revisarlo "con todos los editores" y el causante era sistémico, no de un solo campo.

  Verificado con `ngc --noEmit` limpio; `git status --short` sin credenciales sueltas. Pendiente de que el usuario lo pruebe.

- [x] `RulesEngineService` (`packages/shared-common/src/rules-engine/rules-engine.service.ts`) ahora tolera la sintaxis legacy de `FGetRateValue` tal como el usuario la está pegando al migrar fórmulas del esquema anterior (ej. `entity."FGetRateValue"('PetFunFacEdad','DogAge',null,null,null,null)`, con prefijo de esquema, comillas dobles alrededor del nombre de función, y `NULL` en minúsculas). Motivo: el usuario confirmó que está reutilizando fórmulas migradas tal cual, con reglas que llaman a `FGetRateValue` con esa sintaxis, y que **`FGetRateValue` es la única función de BD legacy que usa** entre las fórmulas migradas (confirmado explícitamente, no asumido) -- por eso se limitó el alcance a tolerar solo esa función, sin construir un normalizador genérico para funciones legacy no usadas. Cambio: `substituteRateValueReferences` pasó de reconocer solo `FGetRateValue(...)` a reconocer también `(?:esquema.)?"?FGetRateValue"?(...)` vía regex extendida -- no cambia ningún valor calculado ni el algoritmo, solo la flexibilidad de reconocimiento de la llamada; la detección de `NULL` (`parseSqlArg`) ya era case-insensitive de antes, confirmado leyendo el código, así que los argumentos `null` en minúsculas del usuario ya funcionaban sin cambios. Verificado: (a) `tsc -p tsconfig.json --noEmit` limpio en `packages/shared-common`; (b) se agregó un caso de regresión real a `scripts/verify-rules-engine.js` (`RECARGOEDAD_LEGACY`) que evalúa la fórmula exacta del usuario (`68+(68*entity."FGetRateValue"('TARIFA_EDAD','EDAD',null,null,null,null))`) de punta a punta contra `RulesEngineService.evaluateChain` -- pasa junto con todos los casos preexistentes (incluida la forma bare original, para no romper compatibilidad); (c) documentado en `packages/shared-common/README.md`. `git status --short` revisado, sin credenciales sueltas.

- [x] Motor de atributos personalizables ("custom fields") conectado a la pantalla de Cotización (`apps/backoffice/features/quotes`), a raíz de un bug real reportado por el usuario al configurar un producto de mascotas: al cotizar, `underwriting-service` devolvía "No se encontró valor de tarifa para los factores consultados". Diagnóstico primero, sin adivinar: se agregó logging temporal a `PrismaRateValueResolver` (`packages/database/src/repositories/rate-value.resolver.ts`, sigue en el código -- pendiente decidir si se deja como diagnóstico permanente de bajo ruido o se retira) que, ante 0 o más de 1 fila de `SRateValue` encontrada, loguea los factores buscados entre corchetes junto con todas las filas reales de esa `SRateTable`. El log confirmó `Factor1=[0]` -- el fallback silencioso de `AttributeValueResolver.resolveAttributeValue` cuando un campo personalizado del riesgo (`PetAccFacRaza`, "Raza") no tiene valor cargado -- y el usuario mismo identificó la causa raíz: el wizard de Cotización nunca pedía ni mandaba ningún dato de raza, porque no existía ninguna pantalla que renderizara los campos personalizados de un riesgo. Investigación de solo lectura, contra datos reales, antes de tocar código: dos scripts nuevos (`packages/database/scripts/investigate-attribute-property-schemas.js` contra el esquema legado `entity`, `investigate-real-attribute-config.js` contra `ars_platform` real, mismo patrón que `investigate-state-machine-config.js`) confirmaron de punta a punta la cadena `SModelAttribute` (qué entidad -- siempre `SRiskProduct` en la práctica -- tiene campos personalizados) → `SAttributeProperty` (el campo concreto de un producto, con `AttributeContent` como JSON-en-varchar) → `SAttribute` (concepto reutilizable, ej. "Raza del Perro") → `SFieldDictionary`/`SFieldValue` (catálogo vigente de valores seleccionables), y confirmaron contra 19 filas reales el contrato exacto de `AttributeContent` para los 6 tipos que existen (`text`, `number`, `date`, `select`, `radio`, `checkbox`): `{name, label, type, validators: [{validationName, aditionalProps?}], validationMessages: {[validationName]: mensaje}, options?: [{key, value}]}`. **Hallazgo real, no asumido**: las `options` embebidas en `AttributeContent` están desactualizadas/incompletas frente al catálogo vigente de `SFieldValue` -- "Raza" embebe solo 13 de las 66 razas reales (con un duplicado exacto, "Great Dane"), un campo de tipo de vehículo embebe 3 de 5 (falta una opción real y sobran dos filas de prueba) -- excepto cuando el diccionario no tiene ninguna fila viva (ej. "¿Esterilizado?"), donde las opciones embebidas son literales booleanos (`{key:"Si",value:true}`/`{key:"No",value:false}`), no referencias a `SFieldValue`. Decisión de diseño justificada con esta evidencia: al armar el schema de un campo `select`/`radio`, preferir siempre las filas vigentes de `SFieldValue` cuando el diccionario tenga alguna activa, y recién ahí caer al array embebido -- así el valor que se guarda en `RiskAttributeValue` es siempre un `IdeFieldValue` real y resoluble por `PrismaAttributeValueResolver` al tarificar. Nuevo endpoint de backend que compone este schema del lado del servidor: `GET /reference-data/model-attributes/by-reference/:ideReference/schema` (`ModelAttributesController.getSchema` / `ModelAttributesService.getSchemaForReference`, sin colisión de ruta con el `:id` existente por tener distinta cantidad de segmentos). Frontend: `RiskAttributesService` nuevo (cliente HTTP contra ese endpoint) y `CreateQuoteRiskPayload` extendido con `riskAttributeValue?: Record<string, unknown>` (opcional -- un riesgo sin motor de atributos configurado no manda nada), y una reescritura sustancial de `QuotesComponent`/`quotes.component.html`: por cada riesgo agregado en Etapa 1 se pide su schema y se arma un `FormGroup` propio (`riskFieldsMap`, clave `CodRiskProduct`, controles con clave `IdeAttributeProperty` -- así `getRawValue()` ya sale en la forma exacta que espera el backend, sin remapeo), renderizado inline debajo del riesgo con un control por tipo (`p-select`/`p-radiobutton`/`p-checkbox`/`input` de texto, número o fecha según corresponda) y validadores mapeados 1:1 desde `validators` (`required`/`min`/`max`/`maxLength`) con los mensajes de error que manda el propio backend (`validationMessages`) -- una regla de validación no reconocida se ignora sin romper el formulario, para no acoplar el frontend a cada nombre posible. "Cotizar" (`canSubmit`) ahora exige además que todos los formularios de riesgo activos estén cargados y sean válidos, y `submit()` incluye `riskAttributeValue` en el payload de creación de la cotización. Verificado: `tsc --noEmit` limpio en `reference-data-service`, `ngc --noEmit` limpio en toda `apps/backoffice`, `git status --short` sin credenciales sueltas. **Pendiente**: que el usuario reconstruya/reinicie los servicios afectados (`reference-data-service` y el build de Angular) y pruebe de nuevo la cotización del producto de mascotas de punta a punta para confirmar que el error original queda resuelto; y decidir si otros riesgos/productos necesitan ajustes no previstos (ej. nombres de `validationName` todavía no vistos).

- [x] `RulesEngineService`/`formula-expression.ts` ahora soportan `round(x)`/`round(x, decimales)` dentro de una fórmula, a raíz de un segundo bug real reportado por el usuario al cotizar el producto de mascotas (una vez resuelto el bug de "Raza" del punto anterior): `Expresión de regla no soportada: identificador "round" sin resolver`. El evaluador de expresiones propio (`formula-expression.ts`) reemplaza el `EXECUTE` de SQL dinámico del sistema legado, pero hasta ahora solo reconocía números/booleanos/operadores -- ninguna llamada a función genérica había aparecido todavía en una fórmula real. Investigación antes de codear, mismo criterio que con `FGetRateValue`: un script nuevo de solo lectura (`packages/database/scripts/investigate-round-formulas.js`, corrido por el usuario contra `ars_platform` real -- mi entorno de trabajo no tiene salida de red hacia ese host) mostró las 62 filas reales de `SCalculationRule.FormulaJSON` que usan `round`, y las 62 tienen exactamente la misma forma: `round(<expresión con rule(...)>, 2)` (ej. `round(rule('Rule13')+(rule('Rule13')*0.02)+(rule('Rule13')*0.05),2)`) -- confirmando que es la función SQL estándar `ROUND` de Postgres, no una función de negocio propia, así que no hizo falta confirmar nada contra código PL/pgSQL específico de esta aplicación, solo replicar el `ROUND(numeric, integer)` real de Postgres. **Detalle técnico no trivial, verificado explícitamente**: `ROUND` de Postgres redondea "half away from zero" (0.5 se redondea siempre alejándose de cero, ej. `2.5`->`3` y `-2.5`->`-3`) -- DISTINTO del "banker's rounding"/half-to-even de otros motores, y también distinto de `Math.round` nativo de JavaScript para negativos (`Math.round(-2.5)` da `-2`, no `-3`). La implementación (`roundHalfAwayFromZero` en `formula-expression.ts`) además corrige un artefacto clásico de punto flotante de JS (`1.005 * 100` da `100.49999999999999`, no `100.5`, lo que sin corrección redondearía mal para abajo) con la técnica estándar de `toPrecision(15)` antes de `Math.round`. `round` se agregó como parte de la gramática del propio parser (tokenizer reconoce `ROUND`/`,` como tokens nuevos, el parser agrega un nodo `round` con valor + decimales opcional, default 0 decimales igual que el `ROUND(numeric)` de un solo argumento de Postgres) -- no como una sustitución de texto antes de evaluar, a diferencia de `FGetRateValue`/`rule()`, porque `round` es parte de la expresión aritmética en sí, no una referencia externa a resolver. Verificado: `tsc --noEmit` limpio en `shared-common`, `underwriting-service` y `product-rating-service`; 7 casos nuevos de regresión agregados a `scripts/verify-rules-engine.js` (la forma exacta de las 62 fórmulas reales, `round` con y sin segundo argumento, redondeo de negativos, los artefactos de punto flotante mencionados, y una cadena completa `rule('COD')` + `round(...)` de punta a punta) -- los 19 casos (12 preexistentes + 7 nuevos) del script pasan. `git status --short` sin credenciales sueltas. Documentado también en `packages/shared-common/README.md`.

- [x] Dirección y teléfono móvil de Tomador y Titular, requisito de emisión detectado por el usuario al revisar el paso "Personas" de Cotización/Contratación ("durante el proceso de emisión de una póliza otros campos son requeridos, como por ejemplo la dirección y el teléfono móvil") -- el backend real ya tenía el CRUD completo desde la fase de `party-service` (`TAddress`/`TContactData` vía `PersonsService.addAddress`/`addContactData`, `POST/PATCH /persons/:id/addresses` y `/contact-data`), pero el paso "Personas" del wizard nunca los usaba: solo buscaba/creaba `TPerson` y asociaba el rol, sin pedir dirección ni contacto. Tres decisiones explícitas del usuario (`AskUserQuestion`): (1) se piden para **Tomador y Titular**, no solo uno; (2) **se bloquea en frontend Y backend** -- no alcanza con que el wizard no deje avanzar, un llamado directo a la API tampoco debe poder generar el contrato sin estos datos; (3) el `CodContactClass` real de "Teléfono móvil" en su catálogo (`SContactClass`, pantalla Catálogos → Tipos de contacto) es **`MOBILE_PHONE`** -- confirmado explícitamente, no adivinado (ver `MOBILE_PHONE_CONTACT_CLASS` en `persons.service.ts` del frontend). Backend: `ContractsService.assertPersonsReadyForIssuance` (nuevo, `underwriting-service/src/contracts/contracts.service.ts`) se llama al inicio de `create()`, después de validar que la cotización esté en estado "Aceptado" -- por cada persona en rol `TOMADOR`/`TITULAR` (mismo filtro que ya usaba `setContractPersons`), cuenta `TAddress` activas y `TContactData` activas de clase `MOBILE_PHONE`; si falta alguna, `ConflictException` con el detalle de qué persona y qué dato falta. Deliberadamente NO exige que existan personas asociadas si la cotización todavía no tiene ninguna (mismo comportamiento tolerante que `setContractPersons`, no se introdujo un requisito nuevo ahí). `QuotesService.listPersons` (`GET /quotes/:id/persons`) se extendió para anidar `TAddress`/`TContactData` (activos, mismo filtro que `PersonsService.findOne`) dentro de cada `TPerson`, así el frontend no necesita un round-trip aparte por persona para saber si le falta algo. Frontend: `PersonsService` (`features/quotes/persons.service.ts`) ganó `addAddress`/`addContactData`/`addMobilePhone` y las interfaces `PersonAddress`/`PersonContactData` (mismo shape que ahora devuelve `listPersons`); `QuotesComponent` agrega, por cada slot de persona (Tomador/Titular), un `contactForm` propio (dirección línea 1 -- obligatoria, línea 2 -- opcional, código postal -- obligatorio, teléfono móvil -- obligatorio) que se muestra recién una vez que la persona ya está asignada -- si ya tiene ambos datos completos se muestra un aviso de "completo" en vez del formulario. `canContinueToSummary()` ahora exige, además de tener Tomador y Titular asignados, que `personContactComplete()` sea verdadero para ambos -- así el wizard no deja avanzar a "Resumen" (y por lo tanto tampoco llegar al botón "Generar contrato") sin estos datos, coherente con el bloqueo del backend. Verificado: `tsc --noEmit` limpio en `underwriting-service`, `ngc --noEmit` limpio en toda `apps/backoffice`, `git status --short` sin credenciales sueltas. **Pendiente de que el usuario pruebe en pantalla**: completar dirección/teléfono para Tomador y Titular de una cotización real y confirmar que "Generar contrato" ahora sí lo exige (y que antes de este cambio no lo pedía).

- [x] `ContractsService.create()` ahora envuelve TODA la cascada de contratación en una transacción de Postgres (`this.prisma.$transaction`), a raíz de un bug real reportado por el usuario: al contratar, faltaba `SStateRule.IndInitialState` para la entidad `TContractRequirement` (fixture de estados incompleto en su BD real, ya corregido por él), lo que hacía fallar `copyRisksAndCoverages` -- pero como el resto de la cascada corría en operaciones de Prisma separadas (decisión explícita documentada en el propio `create()`, "una futura revisión podría envolver esto en `$transaction` si se confirma que el original es atómico"), el `TContract` (y `TContractPerson`/`TContractDistributionChannel`/`TContractBilling`/`TContractFile`/`TContractOperation` ya creados antes de la falla) quedaban huérfanos en la BD real, y como la precondición de `create()` revisa `quote.TContract.length > 0`, reintentar "Generar contrato" fallaba incorrectamente con "la cotización ya tiene un contrato asociado" en vez de permitir un reintento limpio. El usuario preguntó explícitamente si ese debía ser el flujo correcto y si hacía falta rollback -- respuesta: no, y el propio archivo ya tenía el precedente exacto de esta solución: `ContractsService.cancel()` ya envuelve su cascada completa en `$transaction` desde una fase anterior, con un doc-comment que documenta el mismo síntoma ya confirmado en la práctica ("un fallo a mitad de camino... dejaba el contrato grabado en la BD real en un estado a medias, sin forma de revertirlo automáticamente"). Se replicó exactamente ese patrón para `create()`: las precondiciones de solo lectura (cotización existe/sin contrato previo/"Aceptado", personas listas para emisión vía `assertPersonsReadyForIssuance`) siguen corriendo ANTES de abrir transacción; desde `buildContract` en adelante, toda la cascada (`buildContract`, `setContractPersons`, `setContractDistributionChannel`, `setContractBilling`, `createContractFile`, `createInitialContractOperation`, `copyRisksAndCoverages`, `createInitialMovements`, `setNetPrime`, `generateReceipts`, `activateContractTree`) corre dentro de un único `tx: Prisma.TransactionClient`, mismo mecanismo de parámetro opcional con default `= this.prisma` que ya usaban `setNetPrime`/`generateReceipts`/`createContractOperation`/`applyStateCascade` desde la fase de `cancel()` (por eso la mayoría de esas piezas no necesitaron cambios). `this.stateMachine`/`this.rulesEngine` se dejan fuera de la transacción a propósito, mismo razonamiento ya documentado en `cancel()`: solo leen catálogo que esta cascada nunca escribe. El paso final de la cascada (`QuotesService.transitionState(quote.IdeQuote, 'Contratar', actor)`, que marca la cotización de origen como convertida) también necesitaba participar de la misma transacción para que la atomicidad sea completa -- `QuotesService.transitionState` ganó el mismo parámetro `tx` opcional; cuando se lo invoca con un `tx` explícito (el caso de `create()`) se omite el `buildPricingResult` final, que leería por la conexión no-transaccional (`this.prisma`) y no reflejaría de forma confiable escrituras todavía sin commit -- ningún llamador que pasa `tx` usa ese valor de retorno. Timeout de la transacción: `CREATE_TRANSACTION_TIMEOUT_MS` (2 minutos) -- más corto que el de `cancel()` (`CANCEL_TRANSACTION_TIMEOUT_MS`, 10 minutos) porque acá no hay un prorrateo día a día de toda la vigencia como en `setCancelPrime`, pero se deja generoso por los mismos motivos (pooler remoto, varios round-trips por `RulesEngineService.evaluateChain` en `createInitialMovements`). Decisión de diseño confirmada explícitamente por el usuario vía pregunta directa antes de implementar (dado que toca el flujo de mayor riesgo del proyecto). Verificado: `tsc --noEmit` limpio en `underwriting-service`, `git status --short` sin credenciales sueltas. Adicionalmente, se creó `packages/database/scripts/cleanup-orphaned-contracts.js` (solo-lectura por defecto, requiere `--confirm` para borrar) para que el usuario limpie -- corriéndolo él mismo contra su BD real, ya que este entorno de trabajo no tiene salida de red hacia ese host -- el contrato huérfano puntual que ya quedó grabado con el código viejo (identifica contratos con `TContractFile` pero sin ningún `TFileRisk`, firma exacta de este bug, y los borra en el orden correcto sin tocar `TPerson.IndClient`). **Pendiente de que el usuario corra el script de limpieza y confirme en pantalla que el reintento de "Generar contrato" funciona de punta a punta.**

- [x] Corregido un bug de visibilidad transaccional que el fix anterior (envolver `ContractsService.create()` en `$transaction`) había introducido como efecto colateral, encontrado en producción por el usuario ("No se encontró valor de tarifa para los factores consultados." / `PetVetFacEdad` durante la generación del contrato, nunca durante la cotización). Diagnóstico hecho sin acceso directo a la BD real (este entorno de trabajo no tiene salida de red hacia ese host) a partir de tres rondas de logs de diagnóstico temporal que el usuario reprodujo y pegó: (1) el log ya existente de `PrismaRateValueResolver` mostró que `FGetRateValue("PetVetFacEdad", Factor1=[0])` buscaba `Factor1="0"` contra filas reales `DogAge01`..`DogAge10` -- un `"0"` que solo puede venir del fallback silencioso de `FGetValueAttribute` cuando no encuentra el dato; (2) un log nuevo agregado a `PrismaAttributeValueResolver` confirmó que `RiskAttributeValue` venía `null` al leer el `TFileRisk` recién creado; (3) un log nuevo agregado a `ContractsService.copyRisksAndCoverages`, en el momento exacto de la copia `TQuoteRisk` -> `TFileRisk`, confirmó que el dato de origen (`TQuoteRisk.RiskAttributeValue`) SÍ estaba completo y correcto -- descartando un bug de copia o una cotización vieja/incompleta (el usuario incluso repitió la prueba con una cotización nueva, mismo resultado). Causa raíz real: `copyRisksAndCoverages` crea el `TFileRisk` con `tx.tFileRisk.create(...)` dentro de la transacción abierta por el fix anterior, todavía sin *commit*; pero `PrismaAttributeValueResolver`/`PrismaRuleValueResolver` (equivalentes a `FGetValueAttribute`/`FGetValueRule`) leían por su propia conexión Prisma no-transaccional (`this.prisma`, inyectada aparte) -- bajo el aislamiento READ COMMITTED de Postgres, esa conexión separada no puede ver filas que otra transacción todavía abierta escribió pero no confirmó, así que la fila le parecía inexistente (no un problema de datos: el JSON estaba bien, la fila entera era invisible). `PrismaRuleValueResolver` tiene la misma vulnerabilidad estructural para `TMovementConcept` (referencias `rule('COD')` a otra cobertura), aunque no se había manifestado todavía en un fallo observado. `PrismaRateValueResolver` NO tiene este problema -- solo lee catálogo (`SRateTable`/`SRateValue`), que esta cascada nunca escribe. Fix: se agregó un campo opaco `dbTransaction?: unknown` a `EvaluationContext` (en `packages/shared-common/src/rules-engine/rules-engine.service.ts`) y a las interfaces `AttributeValueResolver.resolveAttributeValue`/`RuleValueResolver.resolveRuleValue` (en `calculation-rule.interface.ts`) -- opaco a propósito porque `shared-common` está deliberadamente desacoplado de Prisma/ORM (mismo patrón ya documentado para `StateRuleRepository`), así que `RulesEngineService` solo reenvía el handle, nunca lo interpreta. Las implementaciones reales en `@ars-platform/database` (`PrismaAttributeValueResolver`, `PrismaRuleValueResolver`) lo castean a `Prisma.TransactionClient` y arman `const db = dbTransaction ?? this.prisma`, usando `db` en vez de `this.prisma` en toda lectura -- si no viene `dbTransaction` (caso de cotización, que nunca corre dentro de una transacción), el comportamiento es idéntico al de antes. Único call site que necesitaba pasar el handle: `ContractsService.createInitialMovements`, que ahora pasa `dbTransaction: tx` al invocar `evaluateChain`; confirmado por `grep` que es el único de los dos call sites de `evaluateChain` en todo el código que corre dentro de una transacción (el otro, en `QuotesService.calculateQuoteCoverageConcepts`, no está envuelto en `$transaction`, así que no lo necesita). Nota de proceso importante para futuros cambios en `shared-common`/`database`: ambos paquetes se consumen vía `dist/` (compilado, `main`/`types` en su `package.json`), NO vía los fuentes en `src/` -- después de editar cualquiera de los dos hace falta correr `npx tsc -p tsconfig.json` (sin `--noEmit`) en cada uno para regenerar su `dist/` antes de que `tsc --noEmit` en un servicio consumidor (o el propio backend en ejecución) vea el cambio; si no, el consumidor sigue viendo los tipos/código viejos aunque el `.ts` fuente ya esté corregido (así se manifestó primero como un error de compilación real en `underwriting-service`, resuelto al reconstruir ambos `dist/`). Verificado: `tsc --noEmit` limpio en `packages/shared-common`, `packages/database` y `services/underwriting-service` (los tres tocados), `git status --short` sin credenciales sueltas. **Confirmado por el usuario en pantalla (22/09/2026): generó un contrato de una cotización aceptada y en el log solo aparece el diagnóstico de `copyRisksAndCoverages` -- ya no aparecen los warnings de `PrismaAttributeValueResolver` (`RiskAttributeValue=null`) ni de `PrismaRateValueResolver` (`No se encontró valor de tarifa...`), confirmando que el fix funciona de punta a punta. Por decisión explícita del usuario, los tres bloques de log de diagnóstico temporal (`PrismaRateValueResolver`, `PrismaAttributeValueResolver`, `copyRisksAndCoverages`) se retiraron una vez confirmada la causa raíz -- "de ocurrir, volveremos a ponerlos". Se removieron también los campos `logger`/import de `Logger` que habían quedado sin otro uso en `PrismaRateValueResolver`, `PrismaAttributeValueResolver` y `ContractsService`. Verificado: `tsc --noEmit` limpio en `packages/database`, `services/underwriting-service` y `services/product-rating-service`; `rate-value.resolver.ts` quedó byte a byte igual al commit (el diagnóstico era su único cambio sin confirmar).**

- [x] Listado/recuperación de cotizaciones cerrado, el pendiente que quedaba anotado en Fase 2 (ver el punto de más arriba, "Listado/recuperación de cotizaciones: `underwriting-service` hoy solo expone `GET /quotes/:id`..."). Backend (`services/underwriting-service/src/quoting`): `GET /quotes` (plural, `QuotesController.findAll`/`QuotesService.findAll`), con `ListQuotesDto` -- mismo patrón `page`/`limit` + `{ items, total, page, limit }` ya usado en `iam-service` (`ListUsersDto`/`UsersService.findAll`), no había un helper de paginación compartido en el monorepo. Filtros soportados: `codProduct`, `codState` (resueltos a `IdeProduct`/`IdeState` igual que el resto del módulo, ver `resolveProduct`/nuevo `resolveState`), `dateFrom`/`dateTo` (sobre `TstCreation`), y `all` (booleano, con `@Transform` manual en vez de `@Type(() => Boolean)` porque `Boolean("false")` es `true` en JS). Decisión explícita del usuario sobre "mis cotizaciones": `TQuote` no tiene columna de vendedor/usuario, pero `UsrCreation` (mismo `actor.code` del JWT que ya graba todas las mutaciones del módulo) alcanza sin tocar el esquema -- por defecto (`all` omitido/`false`) el listado solo trae las cotizaciones creadas por el usuario logueado. Cada fila devuelve lo mínimo para identificarla en una tabla (número, producto, estado, fecha, quién la creó, Tomador si ya está asignado vía `TQuotePerson`/rol "TOMADOR") -- sin coberturas/planes/montos, que solo se piden al abrir una cotización puntual. `buildPricingResult` (`GET /quotes/:id`, ya existente) se extendió con `codState`/`desState`/`contract` (`{ ideContract, numContract } | null` si ya se generó un contrato) para poder decidir a qué paso del wizard saltar al retomar una cotización, sin tocar el resto de su forma.

  Frontend (`apps/backoffice/features/quotes`): la ruta `cotizacion` pasó de apuntar directo al wizard a tener tres hijas -- `''` (`QuotesListComponent`, nueva, landing con la tabla), `nueva` (el wizard en blanco) y `:id` (el wizard retomando una cotización existente) -- orden importante en `app.routes.ts`: `'nueva'` va declarado ANTES que `':id'` porque si no, Angular matchea el segmento literal "nueva" contra la ruta paramétrica primero. `QuotesListComponent` usa `p-table [lazy]="true"` (paginado del lado del servidor, primer uso de este patrón en el frontend -- las tablas de catálogos existentes son todas client-side, `findAll()` sin filtros de `CatalogCrudService`) con filtro por producto (reutiliza el mismo catálogo que ya cargaba el wizard) y el toggle "mostrar todas". Filtro por estado y por rango de fechas quedan soportados en el backend pero SIN UI todavía -- los estados hoy son los códigos de prueba `SEED_BORRADOR`/`ACEPTADO`/`SEED_CONTRATADO` (ver `seed-contract-testing-fixtures.js`, punto ya abierto en el roadmap sobre si son los definitivos), y no había ningún `p-datepicker`/`p-calendar` usado en el resto del frontend como para introducir ese patrón sin más contexto.

  **Alcance de "retomar" una cotización, decisión explícita del usuario (`AskUserQuestion`)**: "ver resumen + continuar el flujo", NO edición completa -- no se reconstruye la Etapa 1 (riesgos/atributos personalizados originalmente elegidos) al reabrir una cotización ya guardada, solo se salta al siguiente paso pendiente según su estado real. `QuotesComponent.resumeQuote()` decide el paso destino con datos verificables, no solo con `codState` (que por sí solo no alcanza para distinguir progreso dentro de "Borrador"): si la cotización ya tiene un `TContract` asociado -> paso 'contract' con `quoteAccepted`/`contractResult` ya en true/informados (reutiliza tal cual la misma vista de "éxito" que ya se mostraba justo después de generar un contrato, sin necesidad de una vista de solo lectura aparte); si está "Aceptado" sin contrato todavía -> paso 'contract', listo para generar el contrato; si sigue en Borrador -> se consultan las personas ya asociadas (`listPersons`) y, con el MISMO criterio que ya usaba `canContinueToSummary()` (Tomador y Titular con dirección/teléfono móvil completos), se salta a 'summary' o a 'persons'. Las etapas 'plans'/'persons'/'summary'/'contract' ya estaban diseñadas para depender solo de datos recién pedidos al backend (`pricing`/`quotePersons`/`summary`, todos poblados por `GET`s existentes) y no del estado local de la Etapa 1 (`selectedRisks`/formularios dinámicos de atributos) -- confirmado leyendo el componente y su template antes de escribir el fix, por eso "retomar" no necesitó ningún cambio en esas etapas más allá de poblar esos mismos signals desde afuera. Limitación conocida, no bloqueante: si una cotización quedara en Borrador ANTES de haber sido cotizada/con planes elegidos (ventana muy corta del flujo real, entre `POST /quotes` y `.../price`), el heurístico la mandaría a 'persons' igual, con `pricing()` sin planes -- no se manejó ese caso borde a propósito, dado lo acotado de la ventana.

  Se agregó además un enlace "← Volver al listado" visible en cualquier paso del wizard (`volverAlListado()`), y el botón "Nueva cotización" del paso final ahora navega a `/cotizacion/nueva` (`irANueva()`) en vez de solo resetear el estado en el lugar, para que la URL siempre refleje qué se está viendo.

  Verificado: `tsc --noEmit` limpio en `underwriting-service`, `ngc --noEmit` limpio en `apps/backoffice`; `git status --short` sin credenciales sueltas. **Pendiente de que el usuario lo pruebe de punta a punta en pantalla** (listar, filtrar por producto, alternar "mostrar todas", abrir una cotización en cada uno de los tres estados -- Borrador sin personas, Borrador con personas completas, Aceptada, Contratada -- y confirmar que cada una aterriza en el paso esperado).

- [x] **Pantallas de listado y detalle de contrato**, cerradas y probadas en pantalla por el usuario el 23/09/2026 -- retoma el pendiente de arriba (convertir el `p-tag` "Contrato `<NumContract>`" del wizard en un enlace real). Construidas en dos rondas:
  - **Ronda 1**: `GET /contracts` (listado paginado/filtrable) y hecho real `GET /contracts/:id`, con la pantalla de detalle en 8 pestañas planas. El usuario probó esta primera versión y pidió explícitamente una pantalla "mucho más completa y compleja": todas las tablas relacionadas al contrato, calcada de la estructura del backoffice viejo -- confirmado con `AskUserQuestion` que `TContractMovement` real es `TContractOperation` (no `TCoverageMovement`) y que `TContractData` no es tabla propia (son campos JSON embebidos).
  - **Ronda 2 (la que quedó cerrada)**: rediseño completo a pedido literal del usuario (columnas y estructura exactas, ver commit). Listado con 10 columnas (`Número`, `Producto`, `Canal inicial`, `Inicio`, `Fin`, `Fec. suscripción`, `Tipo de vigencia`, `Edad`, `Fracción de pago`, `Estado`), orden server-side en 9 de las 10 (`Canal inicial` excluida a propósito -- se resuelve con una consulta manual aparte, sin `orderBy` directo de Prisma) y filtro server-side por número de contrato. Detalle rediseñado como árbol de pestañas de 3 niveles, calcado del diálogo `pages/contract` del backoffice viejo (`[disabled]="disabledFileRisk"`/`[disabled]="disabledRiskCoverage"`): Nivel 1 siempre habilitadas -- Tomador/Titular, Certificados (`TContractFile`), Movimientos (`TContractOperation`, con popup de Recibos por movimiento filtrado por `IdeContractOperation`), Canales de distribución, Requisitos; Nivel 2 "Riesgos" deshabilitada hasta seleccionar un Certificado; Nivel 3 "Coberturas" deshabilitada hasta seleccionar un Riesgo. Diferencia deliberada frente al sistema viejo, pedida así explícitamente: "Movimientos" y "Requisitos" son Nivel 1 acá, no dependen de seleccionar archivo/riesgo primero. Seleccionar una fila habilita Y navega automáticamente a la pestaña siguiente (decisión propia, no bloqueante de revertir). Botón "Atributos" en cada riesgo abre un popup con `TFileRisk.RiskAttributeValue` resuelto a etiquetas legibles reutilizando `RiskAttributesService` (el mismo motor de la Etapa 1 de Cotización), en vez de mostrar el JSON crudo. La info general del contrato (número/estado/vigencia/forma de pago/suscripción) dejó de ser pestaña propia -- el usuario no la pidió en su lista -- pasó a una franja compacta arriba de las pestañas. Columnas monto/tasa/prima de Coberturas leen los escalares propios de `TRiskCoverage` (`Amount`/`Rate`/`Prime`), no `TCoverageMovement` -- confirmado que `Prime` ya llega descontado/recargado desde `TQuoteCoverage` al contratar.
  - **Estándar de tablas, a pedido explícito del usuario ("esto aplica para todas las tablas de la aplicación")**: header con fondo de color y fuente más chica -- aplicado GLOBAL vía CSS (`apps/backoffice/src/styles.css`, tokens de marca en vez del hex literal del backoffice viejo) a TODAS las tablas de la app de una vez, por ser cambio barato y de bajo riesgo; ordenamiento, filtro por columna (`p-columnFilter`) y pie "Mostrando X al Y de Z registros" (`showCurrentPageReport`/`currentPageReportTemplate`), implementados completos solo en las pantallas de contrato (listado + las 7 sub-tablas del detalle) -- decisión explícita del usuario (`AskUserQuestion`, "Solo contratos ahora, resto después") para no frenar el cierre de esta pantalla; retrofit al resto de tablas existentes, ver pendiente más abajo.
  - Verificado `tsc --noEmit` (backend) y `ngc -p tsconfig.app.json --noEmit` (`strictTemplates`, frontend) limpios en ambas rondas. Probado en pantalla por el usuario de punta a punta.
- **Pendiente, sin resolver, no bloqueante -- retrofit del estándar de tablas al resto de la app**: ordenamiento, filtro por columna y pie "Mostrando X al Y de Z registros" quedaron implementados solo en las pantallas de contrato (ver ítem de arriba) -- aplicar el mismo patrón a las tablas ya existentes: catálogos comunes, productos, tarifas, ubicaciones, configuración de menú y listado de cotizaciones. El fondo de color/fuente del header ya es global, no hace falta tocarlo de nuevo.
- [x] **Comisión no generada al canal de distribución en una contratación de prueba**, diagnosticada y resuelta (23/09/2026) -- confirmó la hipótesis: era configuración, no un bug. `packages/database/scripts/investigate-missing-commission.js` (nuevo, solo lectura, replica paso a paso `resolveMainDistributionChannel`/`resolveCommissionPercentage` para decir en qué eslabón se corta -- reutilizable para el próximo caso similar) encontró que "Canal B2C" no tenía NINGÚN `SCommissionTree` cargado -- sin árbol, la cadena completa (árbol -> tabla -> comisión) no tiene de dónde arrancar, `TReceipt.Fee` queda en `0` sin error (comportamiento ya replicado tal cual del original). Cargado con `packages/database/scripts/seed-commission-config-b2c-mascotas.js` (nuevo, idempotente): `SCommissionTree` para Canal B2C -> `SCommissionTable` comodín para todo el producto Mascotas (`IdePlanProductRisk`/`IdeCoveragePlan` en NULL) -> `SCommission` al 10% (placeholder explícito, a ajustar al valor comercial real) sobre el proceso real de `RECEGENE`, vigente en rango amplio. Confirmado por el usuario que el script corrió bien contra la BD real. **No retroactivo**: el contrato ya probado (`CONT-2026-28`) queda con su recibo en `Fee=0`, solo los contratos generados de ahora en adelante sobre ese canal/producto calculan comisión real -- **confirmado por el usuario contratando de nuevo**: recibo `RecPETS-2026-31`, `Prime=95.58`/`Fee=9.11` (10% sobre la `PrimaNeta`, no sobre el total con impuesto -- coherente con el placeholder cargado). Hallazgo de paso: ya existe el CRUD completo de `SCommissionTree`/`SCommissionTable`/`SCommission`/`SCommissionProduct` en `party-service` (`BrokersModule`), pero todavía no hay ninguna pantalla en el backoffice para gestionarlo -- el usuario eligió resolver esto puntual con un script ("rápido") y dejar la pantalla de Comisiones para más adelante, ver ítem siguiente.
- **Pendiente, sin resolver, no bloqueante -- pantalla de Comisiones en el backoffice**: gestionar canales/árboles de comisión/tablas/comisiones (`SCommissionTree`/`SCommissionTable`/`SCommission`/`SCommissionProduct`) hoy solo se puede hacer por script o llamando directo a la API de `party-service` (`BrokersModule`, ya expone CRUD completo) -- no hay ninguna pantalla en `apps/backoffice`. Decisión explícita del usuario (23/09/2026, al resolver la comisión faltante de Canal B2C/Mascotas): resolver ese caso puntual con un script y dejar la pantalla para cuando se priorice.

## Fase 3 — Impacto Social

- [x] Etapa 1 (andamiaje + placeholder), cerrada el 22/09/2026. No existen todavía las fórmulas de negocio reales de SIP (puntos de impacto social), CFP (huella de carbono) y SP (sostenibilidad) -- decisión explícita del usuario: construir primero toda la arquitectura con un cálculo placeholder simple, para no bloquear el trabajo técnico mientras se definen. No hay nada legado que replicar acá (confirmado: ninguna de las 130 tablas introspectadas originalmente tiene rastro de esto).
  - **`SSocialImpactConfig`** (tabla nueva por completo, `packages/database/scripts/setup-social-impact-config-table.js`, idempotente): qué productos participan de Impacto Social. Una fila por producto (`IdeProduct` único), `ConfigJSON` con los parámetros del cálculo (hoy solo `{ pctPrimaAdjustment: number }`) e `IdeState` (Activo/Inactivo, misma convención de máquina de estados que el resto del sistema -- sin necesidad de registrar esta tabla como entidad nueva, mismo criterio que `SPlanProductRisk`).
  - **`social-impact-service`** (nuevo, puerto 3008, scaffoldeado igual que el resto): `SocialImpactConfigModule`, CRUD real de `SSocialImpactConfig` (`GET/POST /social-impact-config`, `PATCH /:id`, `PATCH /:id/state`). Agregado al proxy del gateway (`/social-impact/*`) y a los scripts de arranque.
  - **`SocialImpactCalculatorService`** (`packages/shared-common`, mismo patrón de puerto/adaptador `forRoot()` que el motor de reglas y la máquina de estados): el cálculo en sí. Hoy solo resuelve si el producto participa y devuelve el `pctPrimaAdjustment` placeholder tal cual, sin fórmulas de SIP/CFP/SP reales todavía.
  - **`underwriting-service`**: expone el resultado como campo `socialImpact` en la respuesta de `POST /quotes/:id/price` (`QuotesService.buildPricingResult`), de forma puramente informativa -- **sin acoplar el cálculo base de la cotización al social**, no se toca ningún total de prima real todavía.
  - **Decisión de arquitectura explícita del usuario (2026-09-22)**: por ahora `underwriting-service` consulta el cálculo EN PROCESO (lee `SSocialImpactConfig` directamente vía Prisma y usa `SocialImpactCalculatorService`, mismo patrón que el motor de reglas), **no** con una llamada HTTP real a `social-impact-service` -- se evaluaron las dos opciones explícitamente: dado que ya existe la decisión de Fase 1 de mantener un único esquema Postgres compartido (un solo desarrollador, sin necesidad real de aislamiento entre servicios), se prefirió evitar la complejidad de una llamada real entre servicios (timeouts, reintentos, qué pasa si el servicio está caído a mitad de una cotización) sin que hoy exista una razón concreta. `social-impact-service` ya expone sus propios endpoints reales desde ahora para otros consumidores futuros.
  - Verificado `tsc --noEmit` limpio en los 8 servicios backend, `packages/shared-common` y `packages/database`; `git status --short` sin credenciales sueltas. `packages/database` requirió regenerar el cliente Prisma (`db:pull`/`db:generate`) corridos por el usuario desde su propia terminal -- el sandbox de desarrollo (y la VM local del bridge) no tienen salida de red hacia `binaries.prisma.sh` ni hacia la base de datos real por conexión directa (mismo tipo de restricción ya documentada para la instalación de `@jsverse/transloco`).
- [x] Etapa 2 (fórmulas reales de CFP/SIP + llamada HTTP real), cerrada y validada de punta a punta por el usuario el 23/09/2026 contra el producto de mascotas real (`POST /quotes/:id/social-impact-answers` -> `social-impact-service` -> `emissions.dev` -> persistido en `TQuoteSocialImpactAnswer` -> reflejado en `GET/POST /quotes/:id(/price)`). Decisión explícita del usuario (2026-09-22): investigado si existía alguna API gratuita de "score de impacto social/CFP" lista para usar -- no existe ninguna opción viable gratis para SIP/SP a nivel individual, así que se define una fórmula propia de puntos para eso; para CFP sí se encontró `emissions.dev` (nivel gratis, 500 req/mes, sin tarjeta, utilizable en producción), usado SOLO para el componente eléctrico de la huella (auto y vuelos usan factores públicos estables calculados localmente -- no varían por país como sí lo hace la intensidad de la red eléctrica).
  - **`SSocialImpactScoring`** (tabla nueva, fila única global, `packages/database/scripts/setup-social-impact-scoring-tables.js`, idempotente, con fila default seedeada): la fórmula real completa en `FormulaJSON` -- tramos de CFP (kg CO2/año -> puntos), factores de auto/vuelos, país por defecto para electricidad, pesos de SIP (puntos por hora de voluntariado, bonus por causa recurrente/donaciones), pesos del score combinado, y tramos de score combinado -> % de ajuste de prima. Mismo patrón que `SCalculationRule.FormulaJSON` (fórmula real como JSON, validada con DTOs anidados de `class-validator`, editable sin deploy vía `PATCH /social-impact-scoring`, solo `ADMIN`). Tramos/pesos de partida aprobados explícitamente por el usuario (2026-09-22).
  - **`TQuoteSocialImpactAnswer`** (tabla nueva, en `underwriting-service`, una fila por cotización -- `UNIQUE(IdeQuote)`): respuestas crudas del formulario (`AnswersJSON`) + resultado ya calculado (kg CO2, score CFP, score SIP, score combinado, % de ajuste), para no repetir la llamada a `emissions.dev` en cada recálculo de precio.
  - **`SocialImpactScoringModule`** (`social-impact-service`, nuevo): `GET/PATCH /social-impact-scoring` (config de la fórmula) y `POST /social-impact-score` (cálculo puro de CFP/SIP/score combinado/% de ajuste a partir de las respuestas del formulario -- no persiste nada, decisión del llamador). `EmissionsDevClient` con degradación elegante: si `EMISSIONS_DEV_API_KEY` no está configurada o la llamada falla, el componente eléctrico queda en 0 kg CO2 (`electricityDataAvailable: false`) en vez de romper todo el cálculo.
  - **Llamada HTTP real entre servicios (decisión explícita del usuario, 2026-09-22)**: a diferencia de todo lo demás en el proyecto hasta ahora (mismo Postgres, resuelto en proceso vía puertos de `shared-common`), esta SÍ es una llamada HTTP real -- primer caso de este tipo. Nuevo endpoint `POST /quotes/:id/social-impact-answers` en `underwriting-service` (`QuotesService.submitSocialImpactAnswers`): valida que el producto participe (sigue EN PROCESO, vía `SocialImpactConfigResolver`, no vale la pena una llamada HTTP solo para esa pregunta barata), llama a `social-impact-service` vía `SocialImpactHttpClient` (`fetch` nativo, mismo criterio que `ProxyService` del gateway, sin agregar axios) reenviando el mismo header `Authorization` del usuario (sin credencial service-to-service aparte -- `social-impact-service` valida ese JWT con el mismo `AuthModule`/`JWT_SECRET` que todos los demás servicios), y persiste el resultado (`upsert`, se reemplaza si el usuario vuelve a llenar el formulario). `SocialImpactCalculatorService` (el placeholder de Etapa 1) fue retirado de `packages/shared-common` -- `SocialImpactModule.forRoot()` ahora solo resuelve el puerto `SOCIAL_IMPACT_CONFIG_RESOLVER`.
  - **`buildPricingResult`/`GET /quotes/:id`/`POST /quotes/:id/price`**: el campo `socialImpact` ahora tiene tres formas -- `{ active: false }` (no participa), `{ active: true, answered: false }` (participa, falta el paso del formulario) o `{ active: true, answered: true, kgCo2Year, cfpScore, sipScore, combinedScore, pctPrimaAdjustment }` (ya calculado y persistido).
  - Un solo score combinado expuesto al usuario (no tres separados), decisión explícita ("Un solo score combinado (Recomendado para arrancar)").
  - Verificado `tsc --noEmit` limpio en `social-impact-service`, `underwriting-service`, `packages/shared-common` y `packages/database` (los únicos errores esperados son los dos modelos Prisma todavía no regenerados, `SSocialImpactScoring`/`TQuoteSocialImpactAnswer` -- desaparecen en cuanto el usuario corra la migración de abajo).
- [x] Corregido y confirmado (23/09/2026) -- `EmissionsDevClient` llamaba mal a `emissions.dev`: la primera versión probada por el usuario en pantalla devolvió `403` (`kgCo2Year` salió exacto a auto+vuelos, sin aporte eléctrico -- confirma que la llamada fallaba silenciosamente, degradación elegante funcionando como se diseñó). Estaba mal en tres cosas a la vez, confirmado contra la documentación pública real (`https://emissions.dev/docs/api/electricity/calculate` y `.../fuel/calculate`, mismo patrón en ambos endpoints -- aplica a toda la API): método `GET` (no `POST`), ruta `/v1/electricity/emissions` (no `/electricity/calculate`), parámetros por query string (no JSON en el body). Forma real de la respuesta: `data.attributes.emissions.co2e` (no `emissions.co2e` en la raíz). Corregido en `EmissionsDevClient` y reprobado en pantalla por el usuario: `kgCo2Year` subió de 2804 (sin electricidad) a 3345.62 -- 541.62 kg adicionales para 3000 kWh/año en España (`~0.18 kg CO2/kWh`, coherente con la intensidad real de la red eléctrica española), confirmando que la llamada real a `emissions.dev` ya funciona de punta a punta.
- [x] Paso nuevo del wizard en el backoffice (frontend), cerrado el 23/09/2026 -- `QuotesComponent`/`quotes.component.html` (`apps/backoffice`): nuevo paso `socialImpact` en el wizard de Cotización, entre 'plans' y 'persons' (`continueFromPlans`/`pendingSocialImpact` deciden si se muestra, según `pricing().socialImpact.active/answered`; `resumeQuote` salta directo ahí si el producto participa y todavía no se contestó). Formulario (`carFuelType`, `carKmPerYear`, `electricityKwhMonth`, `flightsPerYear`, `volunteerHoursPerYear`, `recurringCause`, `regularDonations` -- `country` deliberadamente afuera, el backend ya cae a un país por defecto) que llama a `POST /quotes/:id/social-impact-answers` (`QuotingService.submitSocialImpactAnswers`) y, una vez contestado, muestra `combinedScore`/`pctPrimaAdjustment` con un botón para editar (`skipSocialImpact`, deliberadamente NO obligatorio -- se puede omitir y completar después, al retomar la cotización). Todas las etiquetas vía `@jsverse/transloco` (23 claves nuevas en `public/i18n/es.json`), mismo criterio que el resto del backoffice ya internacionalizado. Verificado `ngc -p tsconfig.app.json --noEmit` (`strictTemplates`) limpio y probado en pantalla por el usuario -- ver el bug encontrado y su fix en el punto siguiente.
- [x] **Bug real encontrado por el usuario al probar el paso del wizard (23/09/2026) y motor genérico de recargos/descuentos construido para resolverlo de raíz** -- el `pctPrimaAdjustment` se calculaba y se mostraba bien en el paso del formulario, pero el Resumen seguía mostrando el importe de ANTES del descuento (`QuotesService.getSummary` suma `TQuoteCoverage.Prime`, que nunca se recalculaba después de contestar el formulario). Preguntado explícitamente el alcance del fix, el usuario eligió la opción más completa: **que el descuento se refleje también en el contrato/factura real**, no solo en el Resumen -- y, de entrada, si convenía modelarlo como parte de una gestión genérica de recargos/descuentos en vez de algo específico de Impacto Social. Se optó por lo genérico:
  - **Quinto puerto en el motor de reglas** (`AdjustmentValueResolver`/`ADJUSTMENT_VALUE_RESOLVER`, junto a `CalculationRuleRepository`/`AttributeValueResolver`/`RuleValueResolver`/`RateValueResolver` en `packages/shared-common/src/rules-engine/`): nueva sintaxis `adjustment('COD')` en una fórmula de `SCalculationRule`, sustituida por `RulesEngineService.substituteAdjustmentReferences` exactamente igual que `rule('COD')` o `FGetRateValue(...)` -- un número (%, negativo=descuento, positivo=recargo), `0` si no hay dato. Implementación real (`PrismaAdjustmentValueResolver`, `packages/database`): resuelve `IdeQuote` a partir del riesgo de origen (`TQuoteRisk` en Quote, `TFileRisk`->`TContractFile`->`TContract` en Contract) y, según `codAdjustment`, busca el valor ya calculado por la feature dueña -- hoy solo `'SOCIAL_IMPACT'` -> `TQuoteSocialImpactAnswer.PctPrimaAdjustment`. Un futuro recargo/descuento (fidelidad, multi-póliza, siniestralidad) se agrega como un `case` más ahí, sin tocar `ContractsService` ni `QuotesService` -- el ajuste entra por la fórmula del producto, no por código nuevo en los servicios.
  - **Por qué esto resuelve los DOS bugs (Resumen y contrato) a la vez, sin tocar `ContractsService`**: `ContractsService.createInitialMovements` ya re-evaluaba, al contratar, la MISMA cadena de `SCalculationRule` que usa la cotización (`origin: 'Contract'` en vez de `'Quote'`) -- confirmado leyendo el método completo antes de escribir código. Como el ajuste ahora vive en la fórmula del producto (no en un cálculo aparte), ambos mundos lo resuelven igual, sin ningún parche especial en el servicio de contratación.
  - **Migración de datos** (`packages/database/scripts/apply-social-impact-adjustment-to-prima-total.js`, idempotente, con `--dry-run`): envuelve el `THEN` de las 238 reglas reales (no-SEED) cuyo concepto es `PrimaTotal` en `round((<fórmula original>) * (1 + adjustment('SOCIAL_IMPACT') / 100), 2)` -- sin necesidad de entender el contenido de cada fórmula (hay al menos 3 formas distintas de armar `PrimaTotal` entre los ~50 `SCoveragePlan` configurados). Aplicado a las 238, no solo a mascotas: para un producto que no participa de Impacto Social, `adjustment('SOCIAL_IMPACT')` da `0` en silencio y la fórmula queda matemáticamente igual que antes (no-op) -- deja el sistema listo para que otro producto se sume después sin volver a migrar nada.
  - **`QuotesService.submitSocialImpactAnswers`**: ahora, después de persistir la respuesta, vuelve a llamar a `calculateQuoteCoverageConcepts` (antes solo se llamaba una vez, desde `price()`) para que `adjustment('SOCIAL_IMPACT')` quede reflejado en `TQuoteCoverage.Prime` de inmediato -- sin esto, el Resumen seguía congelado en el cálculo de antes de contestar el formulario. Se hizo `calculateQuoteCoverageConcepts` idempotente (borra los `TQuoteCoverageConcept` existentes de cada cobertura en borrador antes de recalcular) porque, a diferencia de `price()` (que corre una sola vez con todos los planes en borrador), ahora se invoca una segunda vez para la misma cotización -- sin el borrado previo hubiera duplicado conceptos en vez de reemplazarlos. Deliberadamente NO se llama a `price()` completo: `rebuildQuoteRiskPlans`/`populateQuoteCoverages` resetearían selecciones de plan/cobertura que el usuario ya hizo.
  - Verificado `tsc --noEmit` limpio en `shared-common`, `database`, `underwriting-service`, `social-impact-service`; agregado un caso nuevo a `verify:rules-engine` para `adjustment(...)` dentro de una cadena -- todo el suite pasa.
  - **Probado de punta a punta por el usuario contra la BD real (23/09/2026)**: cotización de mascotas con 2% de descuento -- Resumen pasó de 97,53 a 95,58 (97,53 × 0,98 = 95,58, exacto); contratada esa cotización, `TReceipt.Prime` del contrato real quedó en 95,58 -- confirma que el descuento llega también al contrato/recibo, sin ningún cambio en `ContractsService`.
- [x] **Detalle de recargos/descuentos en el Resumen**, cerrado y probado en pantalla por el usuario el 23/09/2026 -- el Resumen mostraba el total ya ajustado pero no desglosaba qué recargos/descuentos se habían aplicado. Diseñado de forma genérica (no acoplado a Impacto Social), ya que el motor de `adjustment('COD')` está pensado para más de un tipo de ajuste:
  - **`AdjustmentValueResolver.listAppliedAdjustments(ideQuote)`** (nuevo método, junto a `resolveAdjustmentValue` -- interfaz en `packages/shared-common/src/rules-engine/calculation-rule.interface.ts`, implementación real en `PrismaAdjustmentValueResolver`/`packages/database`): a diferencia de `resolveAdjustmentValue` (que resuelve un `codAdjustment` puntual durante la evaluación de una fórmula), este toma el `IdeQuote` directo y devuelve TODOS los ajustes aplicados a esa cotización como `AppliedAdjustment[]` (`{codAdjustment, pctPrimaAdjustment}`), pensado para mostrar en pantalla, no para evaluar fórmulas. Hoy solo revisa `TQuoteSocialImpactAnswer`; un futuro `codAdjustment` se agrega ahí con el mismo criterio que el `switch` de `resolveAdjustmentValue`.
  - **Encapsulamiento del módulo del motor de reglas**: `RulesEngineModule.forRoot()` exporta deliberadamente solo `RulesEngineService` (no sus puertos internos), para mantenerlo genérico. Como `QuotesService.getSummary()` necesita `AdjustmentValueResolver` directamente (no para evaluar una fórmula), `underwriting-rules-engine.module.ts` (la config a nivel de servicio) vuelve a proveer y exportar `ADJUSTMENT_VALUE_RESOLVER` una segunda vez, explícitamente, a su propio nivel -- sin romper el encapsulamiento del módulo genérico.
  - **`QuotesService.getSummary()`**: ahora incluye `appliedAdjustments` en la respuesta (vía `Promise.all` junto al resto de los datos del Resumen).
  - **Frontend** (`quotes.component.html`/`.ts`, `quoting.service.ts`): nuevo bloque en el paso 'summary' del wizard, debajo de "Prima total" y antes de Tomador/Titular -- solo visible si `appliedAdjustments.length > 0`, con una etiqueta traducida por código (`appliedAdjustmentLabel`, `case` explícito por `codAdjustment` igual que en el backend, con fallback al código crudo si es uno desconocido) y el texto de porcentaje (reutiliza `socialImpactAdjustmentText`, ya existente), en verde si es descuento y ámbar si es recargo. Dos claves nuevas en `es.json`: `quotes.adjustmentLabelSocialImpact`, `quotes.appliedAdjustmentsTitle`.
  - Verificado `tsc --noEmit` limpio en `shared-common`/`database`/`underwriting-service` y `ngc -p tsconfig.app.json --noEmit` (`strictTemplates`) limpio en `apps/backoffice`.
  - Probado en pantalla por el usuario: Resumen de la cotización de mascotas muestra "Impacto Social -- 2% de descuento sobre la prima" debajo de "Prima total €95.58".
- **Pendiente, sin resolver, no bloqueante -- importe del ajuste (no solo el %) en el detalle del Resumen**: pedido explícito del usuario al validar el punto anterior -- hoy la línea de detalle muestra el porcentaje (ej. "2% de descuento sobre la prima") pero no el importe en moneda que representa ese ajuste (ej. "-€1.95"). Para calcularlo hace falta poder comparar la prima con y sin el ajuste (hoy `TQuoteCoverage.Prime` ya viene con el ajuste aplicado, no se guarda el valor "antes") -- a definir si conviene que `AppliedAdjustment` devuelva también el importe en moneda (requeriría que `PrismaAdjustmentValueResolver` conozca la prima base, no solo el `codAdjustment`) o si se calcula en el frontend a partir de datos ya disponibles.
- **Pendiente, sin resolver, no bloqueante -- SP (sostenibilidad)**: la fórmula de Etapa 2 cubre CFP+SIP; SP quedó fuera a propósito (sin criterio propio definido todavía) -- agregar cuando el negocio lo defina, mismo patrón (`SSocialImpactScoring.FormulaJSON`, sin migración de tabla).

## Fase 4 — Claims + IA

- `claims-service` completo (hoy solo existe la tabla `TClaim*`, sin frontend implementado). Investigado el 22/09/2026 al considerar arrancarlo desde Fase 2 (ver el ítem corregido más arriba, "Corregido (22/09/2026): este ítem en realidad es el alcance completo de Fase 4..."): `services/claims-service` es hoy un scaffold vacío (solo `ConfigModule`/`PrismaModule`/`AuthModule`/`GET /health`, mismo template que cualquier servicio sin implementar), y en el backoffice nuevo no existe ni un cascarón de Siniestros (ni ruta, ni componente, ni entrada de menú -- el "cascarón" que se mencionaba era del backoffice v1, no de este). Modelo de datos ya existente en Postgres, real y con relaciones a contrato/cobertura (no hace falta migrarlo, solo consumirlo): `SClaimType`/`SClaimEvent` (catálogo, 2 tablas) + `TClaim`/`TClaimFile`/`TClaimOperation`/`TClaimRequirement`/`TClaimRisk` (transaccionales, 5 tablas) -- 7 en total, tamaño moderado. **Punto abierto importante para cuando le toque el turno a esta fase, a diferencia de TODOS los módulos migrados hasta ahora**: no se identificó ninguna familia de funciones PL/pgSQL `FClaim*` en el legado (confirmado sin resultados en `docs/`/`packages/database`) -- territorio sin investigar todavía. Antes de diseñar cualquier CRUD/flujo real hay que correr una investigación contra la BD real (mismo criterio ya usado para cotización/contratación/reglas de cálculo, ver `packages/database/scripts/investigate-*.js`) para confirmar si existe o no lógica de negocio de siniestros que preservar, en vez de asumir que es un CRUD simple sobre las tablas ya existentes.
- IA para triage/priorización de siniestros, y exploración de detección de fraude y extracción de datos de documentos.

## Fase 5 — Distribución y omnicanalidad

- Store App B2C/B2B2C, marca blanca, SDK/embebido.
- Notificaciones omnicanal (email/SMS/WhatsApp/push).
- Integración de pasarela de pagos.
- Dashboard de KPIs (cartera, comercial, siniestralidad).
- **Decisión pendiente, sin resolver, no bloqueante -- consentimiento + pago en la emisión de pólizas** (planteada por el usuario el 22/09/2026, justo después de estabilizar la cascada de `ContractsService.create()`): en el sistema viejo la emisión desde backoffice NO existía como canal -- es funcionalidad nueva de esta reconstrucción -- así que hoy `ContractsService.create()` deja la póliza activa de inmediato (`activateContractTree`) sin ningún gate de pago/consentimiento, para cualquier canal. Dos escenarios a resolver cuando le toque el turno a esta fase:
  1. **Emisión desde el backoffice**: (a) emitir en estado "Inactivo/Pendiente de pago" y que un operador la active manualmente tras validar el pago fuera de banda (problema: los consentimientos del cliente quedarían sin capturar); (b) generar un enlace (enviado por correo/SMS desde el backoffice) a una landing donde el cliente completa consentimientos y paga a través de la pasarela; (c) dar ambas opciones y que el operador del backoffice elija caso a caso; o (d) mandar ese mismo enlace ANTES de emitir la póliza, en vez de emitirla ya y esperar el pago.
  2. **Emisión desde la web de ventas** (el "Store App B2C/B2B2C" de esta misma fase, todavía no construido): el flujo de consentimientos + pago debería ir integrado de entrada, no como un parche posterior.
  Recomendación de Claude (dada en la conversación, pendiente de validar con negocio/legal cuando se aborde): construir el enlace de autoservicio con consentimiento + pago como UNA sola pieza reutilizable por los dos canales (backoffice y web de ventas), en vez de duplicarla; y preferir emitir la póliza recién al confirmarse el pago (opción 1.d) en vez de emitirla "pendiente" y activarla después, porque tocar el momento de asignación del número de póliza es un cambio más profundo sobre la numeración heredada de `FContract` y evita pólizas en estados intermedios en la BD. Motivo para NO abordarlo ahora: ya es exactamente el alcance de esta Fase 5 (que ya incluye "Integración de pasarela de pagos" y "Notificaciones omnicanal" como items propios), la Fase 2 todavía tiene paridad funcional real pendiente (`product-rating-service`, `billing-service` más allá de solo-lectura, módulo de Siniestros vacío en el backoffice, listado `GET /quotes`), y las decisiones de fondo (pasarela/PSP a integrar, texto legal de los consentimientos de emisión -- distinto del consentimiento GDPR ya implementado en `party-service`, momento de numeración de póliza) son decisiones de negocio, no solo de ingeniería. El usuario confirmó explícitamente documentar esto y seguir con Fase 2.

---

Cada fase se aborda con su propio plan detallado cuando llega su turno — este roadmap es la vista de alto nivel, no el plan de trabajo día a día.
