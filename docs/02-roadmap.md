# Roadmap por fases

Estado a 2026-09-18.

## Fase 0 — Descubrimiento ✅ (completada)

- Auditoría del backend actual (20 repos, versiones, patrones, duplicación).
- Auditoría del frontend actual (2 apps Angular, madurez, gaps).
- Extracción y análisis de las 44 funciones PL/pgSQL (motor de negocio real).
- Decisiones de arquitectura tomadas (ver `00-arquitectura.md`).

## Fase 1 — Fundación técnica (en curso)

- [x] Estructura del monorepo (`package.json`, workspaces, `tsconfig.base.json`).
- [x] `packages/shared-common` scaffoldeado (máquina de estados y motor de reglas como esqueleto, interceptor de auditoría funcional).
- [x] `services/iam-service` scaffoldeado como plantilla de referencia.
- [x] Esquema `ars_platform` creado en Postgres (aislado de `entity`/v1) a partir del DDL de las 130 tablas.
- [x] `packages/database`: Prisma conectado por introspección (130 modelos generados), `PrismaService`/`PrismaModule` y `PrismaStateRuleRepository` implementados.
- [x] Máquina de estados validada end-to-end contra datos reales (`iam-service`, endpoints de prueba en `/state-machine/*`) — adelantado desde Fase 2 porque era el mayor riesgo técnico.
- [x] Ciclo de desarrollo (`npm run start:iam`: build de libs + build del servicio + `node dist/main.js`) validado y estable. Dos causas raíz encontradas y corregidas en el camino: (1) faltaba `tsconfig.build.json` en el servicio — `nest build` lo busca por convención y sin él compila "0 archivos" en silencio (exit 0, sin `dist`); (2) `StateMachineModule` de `shared-common` necesitaba ser un módulo dinámico (`forRoot(repositoryProvider)`) para que el binding de `STATE_RULE_REPOSITORY`, hecho en el servicio consumidor, quedara en el mismo módulo que `StateMachineService` — Nest no resuelve providers "hacia arriba" en módulos que lo importan. Detalle completo en el `README.md` raíz.
- [x] Autenticación JWT real (`iam-service`): `POST /auth/login` contra `TUser`/`TUserCredential` (bcrypt), guard global en `shared-common` (`AuthModule`/`JwtAuthGuard`) que protege toda ruta salvo `@Public()` — reutilizable por cualquier servicio nuevo con solo importar el módulo. Script `db:seed-admin` para crear el primer usuario de prueba. Validado end-to-end (sin token → 401, con token válido → 200 con datos reales). Tercera causa raíz encontrada en el camino: `ConfigModule.forRoot({ envFilePath: '.env' })` con ruta relativa se resuelve contra el `cwd` del proceso, no la carpeta del servicio — y `start:iam` corre `node dist/main.js` desde la raíz del repo, así que nunca encontraba el `.env` real; se resolvió usando `join(__dirname, '..', '.env')`.
- [x] Gestión de usuarios completa (`iam-service`): CRUD de `TUser`/`TRol` (`/users`, rol `ADMIN`), cambio de contraseña, y recuperación de contraseña por correo (Brevo) con token de un solo uso auto-invalidante (sin tabla nueva en la BD). Guard de roles genérico (`@Roles`/`RolesGuard` en `shared-common`) y puerto de envío de correo (`EMAIL_SENDER`/`EmailSender`, implementación Brevo) reutilizables por cualquier servicio futuro. Doble factor de autenticación diferido deliberadamente a una fase posterior (decisión explícita — v1 tampoco lo tenía).
- [x] Motor de reglas de cálculo real (`shared-common`), equivalente a `FQuoteCoverageConcept`/`FMovementConcept` + `FGetValueAttribute`/`FGetValueRule` (ver `docs/01-especificacion-motor-negocio-actual.md`, §3) — adelantado desde Fase 2 por ser, junto con la máquina de estados, la otra pieza central del sistema y el bloqueante real para `product-rating-service`/`underwriting-service`. El `EXECUTE` de SQL dinámico del original se reemplazó por un evaluador de expresiones propio (tokenizer + parser recursivo-descendente + evaluación de AST, sin `eval`/`new Function`/SQL dinámico — `formula-expression.ts`), sin agregar ninguna dependencia nueva. Sustitución de custom fields por límite de palabra (mejora de seguridad sobre el `strpos`/substring original, documentada en §6 de la especificación) y de referencias `rule('COD')` primero contra lo ya calculado en la misma cadena (en memoria) y solo si no está ahí contra la BD — replica el efecto de que el original persiste cada regla dentro del mismo loop antes de evaluar la siguiente, sin que este motor tenga que escribir en la BD (queda puro y testeable; la aplicación del resultado — actualizar columna o insertar concepto — la hace el servicio consumidor). Implementación real de los tres puertos (`CalculationRuleRepository`/`AttributeValueResolver`/`RuleValueResolver`) contra Postgres en `@ars-platform/database`, mismo patrón `forRoot()` que `StateMachineModule`. Verificado con `npm run verify:rules-engine` (evaluador puro + cadena `PrimaNeta -> Impuesto -> PrimaTotal` con dependencias en memoria, sin BD — no hay todavía `SCalculationRule` configuradas en `ars_platform` para una validación end-to-end contra datos reales; eso llega en Fase 2 con `product-rating-service`).
- [x] `product-rating-service` scaffoldeado siguiendo exactamente la plantilla de `iam-service` (mismo `tsconfig.build.json`/`nest-cli.json`/patrón de `ConfigModule`), con el motor de reglas de cálculo ya conectado a Postgres real (`ProductRatingRulesEngineModule` usando `RulesEngineModule.forRoot(...)` con las implementaciones de `@ars-platform/database`) y expuesto en `GET /rules-engine/applicable-rules` y `POST /rules-engine/evaluate` para probarlo en cuanto existan `SCalculationRule` reales configuradas (hoy `ars_platform` no tiene datos de configuración de negocio, solo estructura). Ver `services/product-rating-service/README.md`.
- [x] Script `db:seed-example-rules` (`packages/database/scripts/`): crea la cadena mínima de configuración que `SCalculationRule` exige (17 tablas de catálogo/producto, todo con códigos `SEED_...`) y tres reglas encadenadas (`PrimaNeta -> Impuesto -> PrimaTotal`), para validar `/rules-engine/*` de `product-rating-service` contra Postgres real sin depender de ningún otro servicio. Idempotente. Sirve también como referencia concreta de qué hace falta para dar de alta una regla, hasta que exista el CRUD real (Fase 2). **Validado end-to-end por el usuario**: `GET /rules-engine/applicable-rules` devolvió las 3 reglas ordenadas por `Order` con su `FormulaJSON` real, y `POST /rules-engine/evaluate` devolvió exactamente PrimaNeta=100, Impuesto=16, PrimaTotal=116 con `columnName: "Prime"` — primera prueba real (no en memoria) de todo el motor de reglas contra Postgres.
- [x] CRUD real de los 8 catálogos "simples" en `product-rating-service` (`SRiskLevel`, `SRisk`, `SRiskType`, `SCurrency`, `SInsuranceArea`, `SInsuranceLine`, `SDeductibleType`, `SLimitType`). Lógica Create/List/Get/Update/SetState concentrada en una clase genérica (`CatalogCrudService<T>`, `src/catalogs/catalog-crud.service.ts`) para no repetirla 8 veces; cada catálogo aporta un servicio delgado que resuelve sus FKs propias por código (`SRisk.codRiskLevel`, `SInsuranceLine.codInsuranceArea`) y, en `SRiskLevel`/`SInsuranceArea`, su jerarquía opcional (`codXParent`). Lectura abierta a cualquier usuario autenticado; creación/edición/cambio de estado restringidos a `@Roles('ADMIN')`. Ver `services/product-rating-service/README.md` para el detalle de rutas.
- [x] CRUD real de las 7 entidades "de dominio" en `product-rating-service` (`SProduct`, `SRiskProduct`, `SPlanProduct`, `SPlanProductRisk`, `SCoverage`, `SCoveragePlan`, `SCalculationRule`) — reemplaza el atajo de `db:seed-example-rules` (que queda como ejemplo/semilla rápida) y es la respuesta definitiva a "cómo se registra una fórmula nueva". Las 4 entidades con `Cod`/`Des` único (`SProduct`, `SPlanProduct`, `SCoverage`, `SCalculationRule`) reutilizan `CatalogCrudService<T>` (extendida con un `include` opcional para traer relaciones resueltas); las 3 que son tablas de unión/configuración sin código propio (`SRiskProduct`, `SPlanProductRisk`, `SCoveragePlan`) se escriben explícitas, mismo estilo que `UsersService` en `iam-service`. `SCalculationRule` valida la forma de `FormulaJSON` (`{ if, then, else }`) al guardar, antes de que el motor de reglas la encuentre mal formada al evaluar. Mismo criterio de permisos que los catálogos. Ver `services/product-rating-service/README.md` para el orden de creación (Producto → RiskProduct → PlanProduct → PlanProductRisk → CoveragePlan → CalculationRule; Coverage es independiente) y el detalle de rutas.
- [x] CRUD real de tablas de tarifa multidimensional en `product-rating-service` (`SRateTable`, `SRateFactor`, `SRateValue`, hasta 5 factores por tabla). `SRateTable` reutiliza `CatalogCrudService<T>` (con `include` anidado para traer sus `SRateFactor` ya ordenados por `numOrder`); `SRateFactor`/`SRateValue` se escriben explícitos (sin `Cod`/`Des` propio). `SRateFactor` valida `numOrder` entre 1 y 5 sin repetir posición dentro de la misma tabla (regla de negocio, no constraint de BD). Mismo criterio de permisos que los catálogos. Ver `services/product-rating-service/README.md`.
- [x] Equivalente exacto a `FGetRateValue`, confirmado contra el código fuente real de la función legacy en Postgres (script nuevo `packages/database/scripts/find-legacy-function.js`, reusable para cualquier función legacy futura). Resultado clave: NO hay criterio de desempate por especificidad — el original exige un calce exacto en `Factor1`, comodín solo del lado de quien llama (no de la fila) para `Factor2..5`, y falla si encuentra 0 o más de 1 fila, en vez de elegir una "más específica". Tampoco filtra por vigencia (`TstInit`/`TstEnd`) pese a que la tabla las tiene — se replicó tal cual. Una sola implementación (`PrismaRateValueResolver` en `@ars-platform/database`) sirve dos usos: el endpoint de prueba `GET /rate-values/lookup` en `product-rating-service` y el puerto `RATE_VALUE_RESOLVER` del motor de reglas. Detalle completo en el README de `product-rating-service`.
- [x] `FGetRateValue` cableado dentro del evaluador de fórmulas (`packages/shared-common`) — una fórmula de `SCalculationRule` ya puede invocarlo con `FGetRateValue('CODTABLE','F1','F2',NULL,NULL,NULL)` (misma firma posicional que el original). Verificado sin BD en `npm run verify:rules-engine`. Con esto, `product-rating-service` cubre el 100% de lo documentado en `docs/01-especificacion-motor-negocio-actual.md` §3.
- [x] Scaffolding de los servicios restantes (`party-service`, `reference-data-service`, `underwriting-service`, `claims-service`, `billing-service`, `gateway`), misma plantilla que `iam-service`/`product-rating-service` (`ConfigModule` con ruta absoluta al `.env`, `AuthModule` global, `GET /health`). Todos compilan limpio con `nest build` (verificado con el toolchain real, no solo tipos) y ya están enlazados en los npm workspaces (`npm run start:<nombre>`/`:watch`, ver root `package.json`). Única diferencia entre ellos: `gateway` NO importa `PrismaModule` — es un BFF sin modelo de datos propio, enruta y agrega respuestas de los otros servicios en vez de consultar Postgres directamente. Sin lógica de negocio todavía en ninguno (eso es Fase 2, ver abajo); cada uno conserva su `README.md` con el alcance funcional ya documentado en Fase 0.
- [x] Pipeline de CI básico en GitHub Actions (`.github/workflows/ci.yml`): en cada push/PR a `main` corre `npm ci`, build de `packages/shared-common`/`packages/database`, build de los 8 servicios y lint/test en todos los workspaces (hoy placeholders donde aún no hay reglas de lint ni tests reales configurados). **Validado end-to-end**: primer run real en GitHub Actions exitoso (https://github.com/ag2plataforma/ars-platform/actions/runs/35069730999), confirmando que `prisma generate` funciona sin problema en el runner (a diferencia del sandbox de desarrollo, que no tiene salida de red hacia `binaries.prisma.sh`). De paso se detectó y corrigió que `package-lock.json` había quedado desincronizado con los 6 servicios scaffoldeados en el commit anterior (nunca se completó un `npm install` con red completa desde que se crearon), lo que habría hecho fallar `npm ci` en CI.
- [x] Entorno de despliegue funcionando end-to-end con `iam-service` (hello world real desplegado). Camino preparado para Oracle + Coolify (ver `docs/03-despliegue-oracle-coolify.md`), pero bloqueado en la práctica por falta de capacidad de Oracle para el shape `VM.Standard.A1.Flex` (`Out of capacity...`, un problema de disponibilidad de Oracle, no de configuración). Decisión (2026-09-18): perseguir en paralelo (a) Render como camino de despliegue real con el mismo Dockerfile (ver `docs/04-despliegue-render.md`) y (b) un reintento automático de la capacidad Oracle vía GitHub Actions, para migrar a Oracle+Coolify si la instancia se llega a conseguir. En el camino se encontró y corrigió un segundo problema real, esta vez de Prisma: los 8 `Dockerfile` (con `node:20-alpine`) chocaban con un bug confirmado de Prisma en Alpine 3.21+ que rompe la detección de OpenSSL (sin fix hasta Prisma 6.1.0) -- se migraron a `node:20-bookworm-slim`. **Desplegado y verificado (2026-09-18)**: `iam-service` en Render (`https://ars-iam-service.onrender.com`), `GET /health` responde 200 y `POST /auth/login` con el usuario admin real devuelve un token válido -- confirma que el contenedor desplegado pega de verdad contra la misma base Postgres real. Reintento de Oracle A1.Flex sigue pendiente de configurar, para migrar más adelante si consigue capacidad.
- [x] Decisión sobre partición del esquema de base de datos por servicio: se mantiene un único esquema compartido (`ars_platform`) para todos los servicios durante toda la transición -- ya es lo que ocurre en la práctica (`iam-service`, `product-rating-service` y `reference-data-service` comparten el mismo cliente Prisma de `@ars-platform/database`) y separarlo no aporta nada hoy (un solo desarrollador, sin necesidad real de aislamiento entre servicios) a cambio de complejidad real (migraciones coordinadas, límites de transacciones cross-schema). Se revisita solo si aparece una razón concreta (no por "prolijidad" arquitectónica) más adelante.

## Fase 2 — Núcleo asegurador (paridad funcional)

- [x] CRUD real de `SFieldDictionary`/`SFieldValue` en `reference-data-service` (su primer módulo de negocio real, `FieldCatalogModule`) -- mismo patrón `CatalogCrudService` que product-rating-service, movido a `@ars-platform/shared-common` en el camino (ya lo consumen dos servicios). **Corrección de alcance sobre lo que decía este ítem antes**: al investigar el esquema real, `SAttribute` NO es un simple catálogo de valores -- es la primera pieza de un motor de atributos personalizables/flujos configurables bastante más grande (`SAttribute` -> `SAttributeProperty` -> `SModelAttribute`, este último enlazado con `SEntity`/`SFlowStep`, y consumido en tiempo de ejecución vía el JSON `RiskAttributeValue` de `TQuoteRisk`/`TFileRisk` -- ver `PrismaAttributeValueResolver` en `@ars-platform/database`, que ya implementa la lectura de ese lado). Implementarlo bien exige su propia investigación dedicada primero (mismo criterio que se usó con `FGetRateValue`: no adivinar la forma), así que queda como un ítem separado más abajo en vez de mezclarlo con el diccionario de campos, que sí estaba autocontenido y ya quedó resuelto.
- [x] Investigación del motor de atributos personalizables (`packages/database/scripts/investigate-attribute-engine.js`, corrido contra la BD real). Hallazgos clave: (1) `FGetValueAttribute` confirma exactamente lo que ya implementa `PrismaAttributeValueResolver` en `@ars-platform/database` -- sin cambios de código ahí, el `attribute('COD')` del motor de reglas ya es correcto contra el original. (2) Cadena real de configuración, con datos de ejemplo: `SModelAttribute` (qué set de atributos aplica a qué entidad/producto, ej. "Perro"/"BikeMountain") -> `SAttributeProperty` (el campo de formulario real por producto, con JSON `{name,label,type,validators,...}`) -> `SAttribute` (concepto reutilizable, ej. "Raza del Perro", ligado a un `SFieldDictionary`) -> `SFieldValue` (resuelve el uuid guardado en `RiskAttributeValue` a un código legible). (3) El hack `replace('#','Bicicleta')` de `FFlowStep` resultó ser código de debug abandonado, no una regla de negocio -- ver `docs/01-especificacion-motor-negocio-actual.md` §5/§6, ya actualizado con el detalle. (4) Confirmado un typo real (`FGeState`) en `FGetProcessFlowByUIDSession`, código roto/no usado. **Decisión de alcance**: separar la parte de *configuración* (`SAttribute`/`SAttributeProperty`/`SModelAttribute`, autocontenida y ya bien entendida) de la parte de *orquestación del wizard* (`SEntity`/`SFlowStep`/`FPInstanceFlow`, que depende de las pantallas reales de cotización que todavía no existen en `underwriting-service`) -- ver los dos ítems siguientes.
- [x] CRUD de configuración del motor de atributos (`SAttribute`, `SAttributeProperty`, `SModelAttribute`, `SEntity` de solo lectura) en `reference-data-service`, módulo `AttributeEngineModule`. Mismo patrón `CatalogCrudService` (las tres tienen forma `Cod`/`Des`/`IdeState`+auditoría) con una capa de resolución de códigos a ids igual que `field-values.service.ts`. `IdeReference` en `SModelAttribute` es polimórfico -- se acepta como uuid crudo, sin validar existencia, documentado así. No se tocó el motor de reglas: `attribute('COD')` ya funcionaba correctamente contra `FGetValueAttribute` real (confirmado en la investigación), esto solo agrega la administración de la configuración que consume.
- [x] CRUD de configuración del flujo/wizard (`SProcessFlow`, `SStep`, `SScreen`, `SFlowStep`) en `reference-data-service`, mismo módulo `AttributeEngineModule`. `SFlowStep` no tiene código propio (identificado por `IdeProcessFlow`+`IdeStepCurrent`+`IndResultOK`+`IdeStepForward`) así que no usa `CatalogCrudService`, se escribe a mano. **Sigue pendiente, deliberadamente afuera de esto**: la EJECUCIÓN del flujo en una sesión de cotización concreta (`FPInstanceFlow`, `FGetNextFlowStep`, `TFlowStepInstance`, el templating real `replace('#', vDesShort)`) -- eso depende de que `underwriting-service` tenga el flujo real de cotización (ver ítem de reemplazar `/rules-engine/*`), construirlo antes sería sobre pantallas que no existen.
- [x] Investigación del motor de cotización real (`packages/database/scripts/investigate-quote-engine.js`, corrido contra la BD real). Hallazgos clave: (1) "vigente" en `FQuoteRiskPlan` para elegir `SPlanProductRisk` = estado inicial Y `now()` dentro de `[TstInitial, TstEnd]`. (2) `FQuoteCoverage` arma los valores por defecto de cada `TQuoteCoverage` desde las columnas `Upper*` (no `Lower*`) cuando el flag `IndFixed*` correspondiente está activo, y preselecciona (`IndSelected`) solo las obligatorias (`IndMandatory`); filtra `SCoveragePlan` por estado "Initial", mientras que `FQuoteCoverageConcept` lo hace por estado "Activo" -- inconsistencia real del original, en la práctica ambos resuelven al mismo `IdeState` porque una `SCoveragePlan` nace ya activa. (3) Confirmado un riesgo real de colisión bajo concurrencia en `FQuote('GETQUOTENUMBER',...)`: correlativo leído de la última fila y parseado con `split_part`+1 en memoria, sin lock -- ver decisión de reemplazo abajo. (4) La rama `QUOTESUMMARY` de `FQuote` tiene dos defectos de sintaxis (coma faltante en un `json_build_object`, alias de tabla pegado al nombre) idénticos en las 3 copias del esquema -- nunca llegó a ejecutarse en producción tal cual está; la función hermana `FGetQuoteSummary` (sintácticamente válida) es la referencia confiable para cuando se implemente el resumen real. (5) Ni la creación de `TQuote`/`TQuoteRisk` ni el toggle de `IndSelected` en `TQuoteRiskPlan`/`TQuoteCoverage` existen como función PL/pgSQL -- eran CRUD directo de la capa LoopBack original, así que se diseñaron de cero en vez de reverse-engineerse (con dos reglas de negocio agregadas explícitamente, ver el ítem de abajo).
- [x] Fase 1 del motor de cotización real en `underwriting-service` (reemplaza los endpoints de prueba `/rules-engine/*` que vivían en `product-rating-service`), módulo `QuotingModule` -- ver `services/underwriting-service/README.md` para el detalle completo de rutas. Cubre: crear cotización (`POST /quotes`), cotizar/re-cotizar (`POST /quotes/:id/price`, equivalente a `FQuote('QUOTEPRICING',...)`: cascada `TQuoteRiskPlan` -> `TQuoteCoverage` -> `TQuoteCoverageConcept` vía el `RulesEngineService` ya existente, sin tocarlo) y seleccionar plan/cobertura (dos reglas de negocio agregadas explícitamente, ausentes como validación en el original: un único plan seleccionado por riesgo, y una cobertura obligatoria no se puede deseleccionar). `NumQuote` ahora se genera con una secuencia real de Postgres (`ars_platform."SeqTQuoteNumber"`, `npm run db:setup-quote-sequence`) en vez de replicar el algoritmo original con riesgo de colisión -- decisión explícita del usuario ("Secuencia real de Postgres" sobre "Replicar tal cual"), mismo formato visible `<CodProducto>-<Año>-<N>` pero `N` ahora es correlativo global, no por producto. Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera de esta fase**: resumen (`QUOTESUMMARY`/`FGetQuoteSummary`) y aceptar la cotización (`FQuote_SetState`) -- ambos dependen de `TPerson`/roles (`TOMADOR`/`TITULAR`), que todavía no existen (`party-service` sigue siendo scaffold); ver ítem siguiente. Tampoco se toca la cascada de creación de CONTRATO (`FContract`, ver §4 de la especificación).
- Migración de `product-rating-service` (productos, coberturas, tarifas) — CRUD de configuración y funciones de consulta completos, incluido el motor de tarifas (`FGetRateValue` cableado en las fórmulas, ver Fase 1).
- Migración de `underwriting-service` (cotización y contratación) — el trabajo de mayor riesgo del proyecto.
- [x] Investigación de personas, roles y consentimiento GDPR en party-service (`packages/database/scripts/investigate-party-service.js`, corrido contra la BD real). Hallazgos clave: (1) `TPerson`/`TAddress`/`TContactData` no tienen función PL/pgSQL propia de escritura (solo `FPerson_GetBy`, de solo lectura) -- eran CRUD directo de la capa LoopBack original, igual que `TQuote`/`TQuoteRisk`. (2) Códigos reales de `SPersonRol` confirmados: `TOMADOR`, `TITULAR`, `BENEFICIARIO`, `ASEGURADO` (antes solo nombres informales en comentarios, sin seed/constante en el repo). (3) `FContractPerson('SETQUOTE',...)` confirma exactamente la regla de `docs/01-especificacion-motor-negocio-actual.md` §4: copia `TQuotePerson` (roles TOMADOR/TITULAR) a `TContractPerson` y recién ahí pone `TPerson.IndClient=true`+`TstRelationshipStart=now()` -- nunca antes, o sea nunca en la fase de cotización. (4) Bug real de precedencia de operadores en `FConsent` (documentado y corregido, ver ítem de implementación): el WHERE real equivale a `IdeProduct IS NULL OR (IdeProduct = X AND Activo AND vigente)`, así que los consentimientos globales se devuelven siempre sin chequear estado ni vigencia.
- [x] Fase 1 de `party-service`: personas, direcciones, datos de contacto, catálogo de roles y consentimiento GDPR -- ver `services/party-service/README.md` para el detalle completo de rutas. `PersonsModule` (`TPerson`/`TAddress`/`TContactData`, con `GET /persons/lookup` equivalente a `FPerson_GetBy`, y `SPersonRol` como catálogo `CatalogCrudService`) + `ConsentModule` (`SConsent`/`TPersonConsent`, equivalente a `FConsent` con el bug de precedencia corregido -- decisión explícita del usuario). Dos reglas de negocio agregadas explícitamente (ausentes como validación en el original): una única dirección/dato de contacto principal por persona (por clase, en el caso de contacto), y aceptar el mismo consentimiento dos veces para la misma persona+cotización es idempotente. Toda persona nace `IndLead=true`/`IndClient=false`, confirmado contra `FContractPerson` real. Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera de esta fase**: brokers (`TBroker`) y el árbol de comisiones (`SCommission*`, no bloquean nada hasta `FReceipt`); y la asociación persona+rol+cotización (`TQuotePerson`), que vive del lado de underwriting-service -- ver ítem siguiente.
- [x] Investigación de `FGetQuoteSummary` y `FQuote_SetState` (`packages/database/scripts/find-legacy-function.js`, corrido contra la BD real). Hallazgos clave: (1) `FGetQuoteSummary` es la función sintácticamente válida a usar como referencia (NO la rama `QUOTESUMMARY` de `FQuote`, con los dos defectos de sintaxis ya documentados) -- arma un JSON con moneda, plan/prima seleccionados (subconsultas escalares que asumen un único plan seleccionado en TODA la cotización, no por riesgo -- limitación real del original, replicada sin "corregirla"), personas TOMADOR/TITULAR (asume como mucho una de cada una por cotización) y por riesgo: producto de riesgo, coberturas seleccionadas y atributos parseados vía `FGetParsedRiskAttribute` (función NO investigada todavía, queda pendiente). (2) `FQuote_SetState` es genérica y reutilizable tal cual: aplica `FGetState('Next', tabla, estado, códigoOperativo)` en cascada completa (`TQuote` -> todos sus `TQuoteRisk` -> todos sus `TQuoteRiskPlan` -> todas sus `TQuoteCoverage` -> todos sus `TQuoteCoverageConcept`, no solo lo seleccionado) -- exactamente el mecanismo que ya expone `StateMachineService.getNextState`. No indica qué código operativo puntual dispara la aceptación real ni la cascada de contrato que le sigue -- eso sigue sin confirmar.
- [x] `TQuotePerson` + resumen + transición de estado genérica en `underwriting-service` (`QuotesController`/`QuotesService`, ver su README para el detalle de rutas). `PUT/GET/DELETE /quotes/:id/persons` (`TQuotePerson`, sin función PL/pgSQL propia -- `TPerson`/`SPersonRol` resueltos directo por Prisma contra el Postgres compartido con `party-service`, sin llamada HTTP entre servicios; regla agregada: asignar un rol reemplaza a quien lo tuviera antes en esa cotización). `GET /quotes/:id/summary` (equivalente a `FGetQuoteSummary`, con `attributes`/`FGetParsedRiskAttribute` deliberadamente afuera -- se expone `riskAttributeValue` crudo). `POST /quotes/:id/state` (equivalente genérico a `FQuote_SetState`, `codOperative` expuesto tal cual sin fijar un endpoint "aceptar"). Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera**: la cascada de creación de contrato (`FContract`/`FContractOperation`/`FContractBilling`/`FCoverageMovement`/`FRiskCoverage`/`FContractPerson`/`FContractFilePerson`, ver §4 de la especificación) -- "el trabajo de mayor riesgo del proyecto", y la investigación de `FGetParsedRiskAttribute` -- ver ítem siguiente.
- [x] Investigación de la cascada de creación de contrato (`packages/database/scripts/investigate-contract-engine.js`, corrido contra la BD real). Confirmado el `FContract` real completo (dispatcher con 5 casos: CONTRACTNEW/CANCELCONTRACT/GETJSONBY/GETNUMBER/SETSTATE, idéntico en las 3 copias del esquema ag2ars/entity/temporal) y todas sus operaciones (`FContractOperation`, `FContractBilling`, `FCoverageMovement`, `FMovementConcept` -- 4 casos: SetRulePrime/SetNetPrime/SetCancelConcept/SetCancelPrime --, `FRiskCoverage`, `FFileRisk`, `FReceipt`/`FReceipt_GetNumber`). Hallazgos clave: (1) mismo riesgo de colisión bajo concurrencia en `FContract('GETNUMBER',...)` que el ya resuelto para `NumQuote` -- misma decisión: secuencia real de Postgres (`SeqTContractNumber`). (2) Los tipos de vigencia no anuales (`TempPack`/`TempDays`/`TempDate`) están sin implementar en el propio original (TODO `*****` en el código real), caen a `+1 año` -- se replica ese placeholder tal cual. (3) `FReceipt` determina `IdeReceiptType` (NEW/REN/SUP) con una lógica que el propio comentario del original marca como defectuosa (`*******REVISAR ESTE PROCESO ESTA MALO*********`) -- no aplica todavía porque esta fase solo cubre el caso "contrato nuevo". (4) Operativos reales confirmados: `'Contratar'` (marca la cotización como convertida, al final de CONTRACTNEW), `'Aceptado'` (precondición de la cotización), `'Activar'` (SETSTATE final sobre todo el árbol del contrato), `'Anular'` (SETSTATE de CANCELCONTRACT, deferred), `'CONTGENE'`/`'RECEGENE'` (códigos de `SOperation` para la operación inicial de generación de contrato/recibo).
- [x] Implementación de la Fase 1 de la cascada de creación de contrato (`ContractsModule`/`ContractsController`/`ContractsService` en `underwriting-service`, ver su README para el detalle completo de rutas y alcance). `POST /quotes/:ideQuote/contract` ejecuta la cascada confirmada: `TContract` → `TContractOperation('CONTGENE')` → `TContractPerson` (copia `TQuotePerson` TOMADOR/TITULAR, marca `TPerson.IndClient`) → `TContractDistributionChannel` → `TContractBilling` → `TContractFile` → `TFileRisk`/`TRiskCoverage`/`TContractRequirement` → `TCoverageMovement`/`TMovementConcept` (reutilizando `RulesEngineService.evaluateChain` con `origin: 'Contract'`, confirmado que no requiere cambios de código) → `TReceipt`/`TReceiptDetail` → SETSTATE("Activar") del árbol completo → `FQuote_SetState('Contratar')` sobre la cotización de origen. `NumContract` vía `SeqTContractNumber` (`npm run db:setup-contract-sequence`). Verificado con `tsc --noEmit` y `nest build` limpios. **Deliberadamente afuera de esta primera pasada** (ver ítems siguientes para lo ya resuelto): `FReceipt('BILLFRACTION')` (stub no-operativo confirmado en el original, sin implementar), contratos colectivos reales (`TContractFilePerson` sin poblar -- no hay dato de origen a nivel de archivo en `TQuotePerson`), y `FMovementConcept('SetNetPrime',...)` (se usa `SetRulePrime`, neto = bruto). El árbol de comisiones (`TBroker`/`SCommission*`) y `FMovementConcept('SetNetPrime',...)` (neto = bruto por entonces) se resolvieron en ítems siguientes.
- [x] Implementación de `CANCELCONTRACT` (`ContractsService.cancel()`) -- confirmada línea por línea contra el `FContract`/`FCoverageMovement`/`FMovementConcept`/`FReceipt` reales. Cascada: valida contrato `Activo` y fecha de anulación dentro de su vigencia -> resuelve operación por el endoso -> por cada `TContractFile` activo: cierre en cascada (`TFileRisk` -> `TRiskCoverage`, movimiento de cierre en 0) -> conceptos de cierre en 0 (`SetCancelConcept`) -> prorrateo día a día de la devolución de prima/comisión/impuesto según `SProductEndorsement.ConditionData` (`SetCancelPrime`) -> `TContract` a `'Modificar'` -> recibo(s) de la anulación -> cascada final de estado a `'Anular'` en las 6 entidades del árbol. Corregido en el camino un bug real de no-determinismo en `PrismaStateRuleRepository.findInitialState` (sin `ORDER BY`, podía devolver un estado inicial distinto entre dos filas `IndInitialState=true` para la misma entidad tras una reclasificación) y un bug de fixture (`TContractDistributionChannel` debía nacer `Activo`, no `Borrador` -- confirmado contra `FReceipt`/`FContract('SETSTATE',...)` reales). **Endurecida con seguridad transaccional**: toda la cascada corre dentro de un único `$transaction` de Prisma (timeout extendido a 10 minutos porque el recorrido día a día de `setCancelPrime` toma 1-2 minutos contra el pooler remoto) -- antes de esto, un fallo a mitad de camino dejaba el contrato en la BD real en un estado a medias sin forma de revertirlo (confirmado en la práctica en esta ronda). `this.stateMachine`/`this.rulesEngine` quedan deliberadamente fuera de la transacción (solo leen catálogo que esta cascada nunca escribe). Verificado end-to-end contra la BD real por el usuario: estado, movimiento de cierre prorrateado, comisión (10%) e impuesto (16%) reales, recibo tipo `SUP`.
- [x] Unificación de la generación de recibos (`FReceipt('NEWCONTRACT'|'CANCELCONTRACT', ...)`, una sola implementación en el original para ambos casos): `generateReceipts` reemplaza tanto al `createInitialReceipt` simplificado de CONTRACTNEW (resumen de una línea, `Fee=0`) como a la versión ad-hoc que se había escrito solo para `CANCELCONTRACT`. Genera SIEMPRE una operación `RECEGENE` nueva y re-apunta a ella TODOS los movimientos en Borrador del contrato, con un detalle de `TReceiptDetail` por CADA concepto de cada movimiento (no un resumen) más el cálculo real de comisión (`SCommissionTree`/`SCommissionTable`/`SCommission` del canal principal vigente) por cada concepto `PrimaNeta` -- `TReceipt.Fee` ya no queda en 0. Corregido en el camino que la comisión por línea no se redondea individualmente (fiel al original, que solo trunca el `Fee` total). Esto exigió dos correcciones más, ambas confirmadas contra el código fuente real: (1) `ContractAge` se persiste en `1` para un contrato nuevo (antes 0) -- confirmado literal contra el `INSERT` real de `TContract` en CONTRACTNEW, no una fórmula a adivinar (la fórmula de INCREMENTO en renovaciones sigue sin confirmar, no aplica todavía); (2) `createInitialMovements` ya no fija `IdeContractOperation` al crear el movimiento inicial (queda `NULL`, confirmado contra `FCoverageMovement` real) -- antes se le asignaba `CONTGENE` directamente, lo que hubiera roto el re-apunte a `RECEGENE` de `generateReceipts`. Verificado end-to-end contra la BD real por el usuario: contrato nuevo con `ContractAge=1`, movimiento re-apuntado a la operación `RECEGENE`, recibo tipo `NEW` con desglose completo por concepto y comisión real (`Fee=10`, 10% sobre `PrimaNeta=100`, coincide con `SCommission.Percentaje` configurado).
- [x] CRUD administrativo del árbol de comisiones en `party-service` (`BrokersModule`: `TBroker`, `SCommissionTree`, `SCommissionTable`, `SCommission`) -- confirmado contra el dump completo de funciones PL/pgSQL que ninguna de las 4 tiene función propia de escritura (solo referencias de solo lectura tipo `FGetIdeDesc('TBroker', ...)`), así que se diseñó de cero como CRUD administrativo, no como réplica de lógica de negocio existente. `TBroker`/`SCommissionTree` reutilizan `CatalogCrudService`; `SCommissionTable` también pero con `findAll` filtrable por `codCommissionTree`/`codProduct` (acepta `idePlanProductRisk`/`ideCoveragePlan` como uuid crudo, tablas de unión sin código propio); `SCommission` se escribe a mano (sin `Cod`/`Des` propio, mismo estilo que `SRateValue`), con `NumMovement` como correlativo de versión por `(IdeCommissionTable, IdeProcess)` calculado automáticamente (máximo existente + 1) -- ya lo consumían en la práctica `resolveCommissionPercentage`/`generateReceipts` de `underwriting-service`, así que este módulo es lo que faltaba para dar de alta comisiones reales sin tocar la BD a mano. Verificado con `nest build` limpio, y luego end-to-end contra la BD real por el usuario: creado un `TBroker` (con `SBrokerType`/`TPerson` resueltos), un `SCommissionTree` sobre `SEED_CHANNEL`, una `SCommissionTable` sobre ese árbol + `SEED_PRODUCT`, y una `SCommission` sobre esa tabla + `SEED_PROCESS` con `Percentaje=10`/`NumMovement=1` -- las 4 relaciones encadenadas correctamente. El split de comisión por canal (`SCommissionProduct`), deliberadamente afuera de esta ronda, se resolvió en el ítem siguiente.
- [x] Investigación de `FContractDistributionChannel('SETQUOTE', ...)` (línea por línea contra el dump completo, líneas ~1136-1260, idéntica en las 3 copias del esquema) y corrección de `setContractDistributionChannel` en `underwriting-service` para que la replique de verdad. Hallazgo clave confirmado: si existe configuración `Activa` en `SCommissionProduct` para (canal de origen de la cotización, producto), se crea un `TContractDistributionChannel` por CADA fila que matchee (con su canal destino, `Percentaje` e `IndMain`) -- el original NO filtra ahí por vigencia ni por `NumMovement` (a diferencia de `SCommission`/`resolveCommissionPercentage`, que sí lo hacen), toma todas las filas activas tal cual, confirmado literal. Si no existe ninguna configuración, se preserva el fallback ya implementado (un único canal, 100%, `IndMain=true`). `NumMovement` es siempre `1` en ambos casos (literal del `INSERT` real). CRUD de `SCommissionProduct` agregado a `BrokersModule` en `party-service` (`GET/POST /commission-products`, etc.) -- **decisión explícita del usuario**: sin versionado (`update()` edita en el lugar) porque la función real no distingue "versión vigente" y dos filas Activas para el mismo (producto, canal origen, canal destino) duplicarían el split en el contrato; `codProduct`/canales no se pueden cambiar en `update()` por la misma razón que en `SCommission` (para eso se crea una fila nueva). Verificado con `nest build` limpio en ambos servicios (pendiente de probarlo end-to-end contra la BD real, igual que el resto de este módulo).
- [x] Investigación e implementación de `FMovementConcept('SetNetPrime', ...)` en `underwriting-service`, confirmado línea por línea contra el código fuente real (idéntico en las 3 copias del esquema). Calcula `TMovementConcept.ConceptNetValue` de los movimientos en Borrador de cada `TRiskCoverage`: para el movimiento INICIAL (único caso que se ejercita hoy, ya que `FReceipt('BILLFRACTION')` -- que crearía movimientos subsiguientes -- sigue sin implementar), la misma fórmula se aplica a TODOS sus conceptos según `SProduct.IndProportionalPrime` -- proporcional a la fracción de pago + recargo (`SProductPaymentFraction.PorSurCharge`), o por días exactos del período de facturación vigente (`TContractBilling`) -- sin redondear, literal del original. También se implementó (código listo, no ejercitado todavía) el caso de movimientos subsiguientes: diferencia prorrateada día a día si la ventana nueva cae dentro de la anterior, o recálculo con la misma fórmula si sigue justo después dentro del período vigente; cualquier otro escenario queda sin tocar (hueco real del original, no inventado). Al terminar, `TCoverageMovement.Prime` se fija con el `ConceptNetValue` de `PrimaTotal`, y `TRiskCoverage.Prime` con el `ConceptValue` BRUTO (no neto) del último movimiento procesado -- confirmado literal, mismo patrón ya visto en `setCancelPrime`/`setCancelConcept`. `createInitialMovements` ya no deja `ConceptNetValue` igual al bruto como valor final (queda como provisorio hasta que `setNetPrime` corre a continuación en la cascada de `create()`). Verificado end-to-end contra la BD real por el usuario: `PrimaTotal` bruto `116` -> neto `9.6666...` (=116/12, fracción mensual `SEED_MENSUAL`) -> `TCoverageMovement.Prime` redondeado a `9.67`; mismo criterio en `PrimaNeta`/`Impuesto` (100/12, 16/12); `TRiskCoverage.Prime` quedó en `116` (el bruto, confirmando que ese campo no se actualiza con el neto -- comportamiento real, no bug). En el camino se encontró y corrigió un bug de fixture real: `SEED_PRODUCT` (creado por `seed-example-rules.js`) nacía sin `TstEnd`, y el `now() between pro.TstInitial and pro.TstEnd` real da NULL/falso con `TstEnd` NULL -- `setNetPrime` terminaba sin hacer nada, dejando `Prime` en el bruto. Corregido `seed-example-rules.js` para que un producto nuevo nazca con `TstEnd: FAR_FUTURE` (mismo criterio que `SPlanProductRisk` ya usaba), con un script aparte (`fix-seed-product-tstend.js`) para poner al día la fila ya existente.
- [x] Primer módulo de negocio real de `billing-service` (`BillingModule`, solo lectura). Antes de implementarlo se encontró que buena parte del alcance original del README de este servicio ya había quedado cubierta en otro lado: `underwriting-service` ya genera `TReceipt`/`TReceiptDetail` reales (con comisión) dentro de la cascada transaccional de creación/cancelación de contrato -- moverlo acá rompería esa atomicidad, así que se queda donde está --, y el CRUD del árbol de comisiones ya se hizo en `party-service` (`BrokersModule`). `TContractBilling` tampoco es un catálogo (se autogenera por contrato). **Decisión explícita del usuario**: en vez de duplicar o migrar esa lógica, `billing-service` expone `GET /receipts`/`GET /receipts/:id` (con desglose completo por concepto) y `GET /contract-billing`/`GET /contract-billing/:id`, consultando lo que `underwriting-service` ya escribe contra el mismo Postgres compartido. El reemplazo real de `ag2paymentgateway` (cobranza/pago de un recibo) queda deliberadamente afuera: no hay tabla de pagos sobre `TReceipt` en el esquema ni es una de las 44 funciones PL/pgSQL investigadas -- sería diseño nuevo, no una réplica, así que espera a una necesidad concreta. Verificado con `tsc --noEmit` y `nest build` limpios.
- [x] Migración del resto de `reference-data-service` (`CommonCatalogsModule`, `I18nModule`, `SetupModule` -- ver su README para el detalle completo de rutas). `CommonCatalogsModule`: ocho catálogos simples (`CatalogCrudService`) más `SCountry`/`SLocation`; esta última escrita a mano porque su unicidad real es compuesta (`CodLocation`+`IdeCountry`, no `CodLocation` solo), a diferencia de `SSiteMap` que sí es único global y reutiliza `CatalogCrudService`. `I18nModule`: confirmado por grep sobre el dump completo de funciones PL/pgSQL (sin resultados para `Translat*`/`GetText*` propias, solo el `pg_catalog.translate` nativo de Postgres) que `STextContent`/`STranslator` no tienen función de negocio que replicar -- CRUD puro, único real por (`IdeTextContent`, `IdeLanguage`) validado antes de insertar. `SetupModule`: `SApplication`/`SApplicationRole`/`SSiteMap` (catálogos) más `SiteMapMenuService`, migración línea por línea de `FGetSiteMap` real (confirmado idéntico en los esquemas `ag2ars`/`entity`/`temporal`; `ag2servicio.FGetSiteMap` es un sistema legado no relacionado con tablas `ARS_APLICACION_MENU*` propias, descartado) -- árbol de 3 niveles filtrado por `SSiteMapRole`/`SApplicationRole` contra una lista de códigos de rol separados por coma, ordenado por `NumOrder`; el SQL dinámico sin parametrizar del original para la lista de roles (riesgo de inyección, ya señalado en la Fase 0) se reemplazó por consultas Prisma parametrizadas. **Desviación deliberada, con visto bueno explícito del usuario**: la función original no tiene `DISTINCT`, así que un usuario con roles que comparten acceso al mismo ítem lo recibe duplicado en el original -- acá se deduplica (consecuencia natural de consultar `SSiteMap` vía Prisma, sin código extra), a diferencia del criterio usado en lógica financiera/de negocio crítica (ej. cálculo de prima), porque el árbol de navegación no lo es. `SSiteMapRole` (tabla puente rol→ítem, única real por `IdeSiteMap`+`IdeApplicationRole`) se escribió a mano, sin `update` (una concesión de acceso se otorga o se retira, no se edita). Verificado con `tsc --noEmit` y `nest build` limpios.
- [x] `gateway` real: `ProxyModule`, proxy simple 1:1 por servicio (`/iam/*`, `/party/*`, `/reference-data/*`, `/product-rating/*`, `/underwriting/*`, `/claims/*`, `/billing/*`), sin agregación -- ver `services/gateway/README.md` para el detalle completo. Alcance decidido explícitamente con el usuario: no hay comportamiento legado que replicar (confirmado en `docs/00-arquitectura.md` que el sistema v1 nunca tuvo un gateway común real -- los frontends le pegaban directo a cada servicio), así que se optó por el proxy simple en vez de adivinar la forma de endpoints agregados que ningún frontend real todavía pide. Reenvía método/query/body/headers (incluido `Authorization`) con el `fetch` global de Node 20, sin agregar `axios` como dependencia. El guard JWT global protege todo por defecto; público solo lo que cada servicio real también expone público (`GET /health` de los 7, y `auth/login`/`auth/forgot-password`/`auth/reset-password` de `iam`, sin los cuales no se podría obtener un token). Errores del downstream se propagan tal cual (status+body reales); `503` con mensaje claro si el servicio de destino no responde. Verificado end-to-end (no solo `tsc`/`nest build`) con dos servidores mock y el gateway real corriendo juntos: rutas públicas/protegidas, reenvío de `Authorization`, prefijo con guion, query string, body JSON, propagación de error `4xx` real y `503` por servicio caído -- los 10 casos probados dieron el resultado esperado. **Deliberadamente afuera**: agregación real de varios servicios en una respuesta (se decide con una pantalla real de alguno de los dos frontends) y upload de archivos (ningún servicio lo expone todavía).
- [x] Primera pasada del frontend nuevo del backoffice -- ver `apps/backoffice/README.md`. Se investigó primero el backoffice v1 real (`Code/Front/ag2backofficewebapp`, repo aparte, Angular 16 + PrimeNG, tema `lara-light-purple`, componente `ag2-table` genérico ligado a la máquina de estados, menú lateral armado contra `FGetSiteMap`) para entender qué había antes de rehacerlo. Decisión explícita del usuario: identidad visual nueva de cero (no se mantiene el violeta `#6A63B8`), pero se conserva PrimeNG como librería de componentes complejos + Tailwind CSS 4 para el layout. **Pinneado a Angular 21 + PrimeNG 21 a propósito, no la última versión**: la primera pasada usó Angular 22 + PrimeNG 22, pero al probarlo en pantalla apareció un cartel real "Invalid PrimeUI License" -- PrimeNG dejó de ser MIT a partir de la v22 (licencia paga o "Community" gratuita pero a trámite, con límites de tamaño de empresa y renovación anual; confirmado contra la página oficial de PrimeTek, `primeui.dev/nextchapter`: "v21 y anteriores quedan MIT para siempre"). Se bajó toda la app a v21 para no depender de la licencia de un tercero -- bonus, Angular 21 pide Node 22.12+ en vez de 22.22.3+, así que de paso se pudo sacar el paso extra de `nvm` que se había agregado antes. Esqueleto real en `apps/backoffice` (Angular 21 standalone, sin NgModules) -- login de dos pasos contra `iam-service` real vía el `gateway` (incluye el 2FA recién implementado), guard + interceptor JWT, layout con sidebar dinámico contra `GET /reference-data/site-map-menu` (real, no simulado) y dashboard placeholder. **Verificado en pantalla por el usuario, de punta a punta**: `npm install`, login real, entrada al dashboard, y confirmado que el cartel de licencia de PrimeNG ya no aparece con v21. En el camino se corrigieron varios errores reales de API de PrimeNG 22 (luego irrelevantes al bajar a v21, pero el patrón usado -- content projection para label/icon de `pButton` en vez de inputs -- sigue siendo válido) y un bug real de orden de inicialización en Angular (`TS2729`) encontrado recién al poder correr el compilador (`ngc`) una vez el usuario instaló las dependencias. Próximo módulo de negocio decidido: Catálogos/diccionario (`reference-data-service`).
- [x] Primer módulo de negocio real del frontend nuevo: pantalla de Catálogos comunes (`apps/backoffice/src/app/features/catalogs`) -- decidido explícitamente con el usuario (entre `CommonCatalogsModule`, `FieldCatalogModule` y `SetupModule`, los tres candidatos razonables detrás de "Catálogos/diccionario"). Un solo componente genérico (`CatalogsComponent`, configurado por `CATALOG_REGISTRY`) reutilizado para los 8 catálogos "simples" (idiomas, géneros, estado civil, profesiones, actividad económica, tipos de identificación, parentescos, tipos de contacto) más `SCountry` (con su idioma por defecto como `p-select`) -- alta, edición, activar/inactivar con confirmación. **Cambio de contrato acompañante en el backend**, aditivo y sin romper nada existente: se agregó `include: { SState: true }` (y `SLanguage: true` para `SCountry`) a los `CatalogCrudService` de `CommonCatalogsModule` -- sin esto, `findAll`/`create`/`update`/`setState` devolvían `IdeState` como UUID crudo, sin forma de que el frontend supiera si una fila estaba "ACTIVO" o no (mismo patrón de `include` que ya usaba `application-roles.service.ts` con `SApplication`, solo que ningún catálogo común lo tenía todavía). `SLocation` queda deliberadamente afuera de este registro -- es jerárquico y su unicidad es compuesta (`CodLocation`+`IdeCountry`), amerita pantalla propia. Verificado con `tsc --noEmit` (backend) y `ngc --noEmit` (frontend) limpios.
- Implementación del módulo de Siniestros en el backoffice (hoy es un cascarón vacío).
- [x] Doble factor de autenticación (TOTP) en `iam-service`, opcional y autoservicio -- ver su README para el detalle completo de rutas. Feature nueva de cero (v1 tampoco la tenía, confirmado), así que sin comportamiento legado que replicar -- diseño decidido explícitamente con el usuario: (1) solo TOTP, sin respaldo por email; (2) opcional autoservicio, no obligatorio para ningún rol; (3) el secreto TOTP se guarda en texto plano (no se puede hashear, y cifrarlo sumaría gestión de una clave de aplicación nueva fuera de alcance ahora). TOTP (RFC 6238 sobre HOTP/RFC 4226) implementado a mano con el módulo `crypto` de Node -- sin librería nueva (`otplib` o similar) porque este entorno de trabajo no puede instalar paquetes ahora mismo (política de red bloquea npm y binaries.prisma.sh, ver más abajo) -- **verificado línea por línea contra los vectores de prueba oficiales de la RFC** antes de integrarlo (no solo `tsc`/`nest build`). Login de dos pasos: `POST /auth/login` devuelve `{requiresTwoFactor, twoFactorToken}` (token de 5 minutos, mismo patrón `purpose` ya usado en recuperación de contraseña) en vez del JWT cuando el usuario tiene 2FA activo; `POST /auth/2fa/verify` completa el segundo paso con un código TOTP o uno de los 10 códigos de respaldo de un solo uso (hasheados con bcrypt). Apagar 2FA exige reingresar la contraseña Y un código válido, no solo estar logueado. **Primera evolución real del esquema en Fase 2** (`TUserTwoFactor`/`TUserBackupCode`, `db/migrations/002_two_factor_auth.sql`): decisión explícita del usuario de seguir con el script SQL simple (mismo patrón de la migración 001) en vez de adoptar Prisma Migrate como planeaba originalmente `db/README.md` -- la BD real es un Postgres remoto compartido (mismo servidor de Vercel que usa `entity`/v1) y `prisma migrate dev` necesita una shadow database ahí, con permisos inciertos; no valía el riesgo para una feature chica. **Limitación técnica superada**: durante el desarrollo inicial no se pudo correr `prisma generate` en el entorno de trabajo de ese momento (la descarga del motor de consultas estaba bloqueada por la política de red, igual que el registro de npm), así que `TUserTwoFactor`/`TUserBackupCode` se accedían con `$queryRaw`/`$executeRaw` parametrizado. El usuario corrió `npx prisma generate` en su propia terminal después de aplicar la migración (y corrigió de paso un error real de sintaxis en `schema.prisma`: los comentarios de bloque `/** */` no son válidos en el DSL de Prisma, solo `//`/`///`), y `TwoFactorService` se refactorizó al cliente Prisma tipado, igual que el resto del proyecto. **Sin cobertura todavía**: recuperación de cuenta si se pierden el celular y los 10 códigos de respaldo a la vez (soporte manual, fuera de alcance de esta primera versión).

## Fase 3 — Impacto Social

- `social-impact-service`: cálculo de SIP (puntos de impacto social), CFP (huella de carbono) y SP (sostenibilidad), y función de ajuste dinámico de primas.
- Integraciones con fuentes externas de datos (huella de carbono, voluntariado) — a definir.
- Exposición del ajuste de prima dentro del flujo de cotización existente en `underwriting-service`, sin acoplar el cálculo base al social (servicio separado, se consulta).

## Fase 4 — Claims + IA

- `claims-service` completo (hoy solo existe la tabla `TClaim*`, sin frontend implementado).
- IA para triage/priorización de siniestros, y exploración de detección de fraude y extracción de datos de documentos.

## Fase 5 — Distribución y omnicanalidad

- Store App B2C/B2B2C, marca blanca, SDK/embebido.
- Notificaciones omnicanal (email/SMS/WhatsApp/push).
- Integración de pasarela de pagos.
- Dashboard de KPIs (cartera, comercial, siniestralidad).

---

Cada fase se aborda con su propio plan detallado cuando llega su turno — este roadmap es la vista de alto nivel, no el plan de trabajo día a día.
