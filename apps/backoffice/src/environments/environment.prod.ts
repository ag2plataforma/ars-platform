export const environment = {
  production: true,
  // Se ajusta al publicar el gateway (mismo criterio que services/*/README.md de despliegue).
  apiUrl: '/api',
  // Mismo criterio que environment.ts: a mano, en sync con package.json.
  appVersion: '0.1.0',
};
